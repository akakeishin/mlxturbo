### fastmlx/kernels/gated_delta_states.py

fastmlx/kernels/gated_delta_states.py:51-52,73-77,170-200 / 壊れる / `n_per_t=Dk/32` が端数を切り捨てるため `Dk%32!=0` では末尾を未処理のまま返し、`hv_idx/(Hv/Hk)` も `Hv%Hk!=0` または `Hv<Hk` で誤った head/OOB を生む。state・mask・q/k/v の形状検証もない / launch 前に全 shape、`Dk%32==0`、`Hv%Hk==0`、state/mask shape を検証し、非対応形状は ops 実装へ fallback する

fastmlx/kernels/gated_delta_states.py:102-107 / 怪しい / mask=false 時だけ32 lane 全てが同じ `y[dv_idx]` へ非atomic書き込みする。同値の0でも Metal 上は同一アドレス競合で、顕在化の有無は要実測 / true 分岐同様 `thread_index_in_simdgroup==0` の1 lane だけが書く

fastmlx/kernels/gated_delta_states.py:65-67,124-127,142-143,199-200,242-245 / 改善 / `state_out` を全量allocate/writeしているが唯一のcallerは `_state_out` を即座に破棄し、同じ最終状態を `states_all[:,-1]` から使う。T=1では状態出力帯域がほぼ二重になる。効果量は要実測 / `state_out` output、pointer、最終store loopを削除し、`states_all` のみ返す

fastmlx/kernels/gated_delta_states.py:73-76,111-114 / 改善 / `s_idx=(Dk/32)*lane+i` は各store命令でlane間が `Dk/32` float離れ、典型的な `Dk=128` ではstride 16 byteになる。`states_all` の追加書き込みがcoalesced最適かは要実測 / `s_idx=lane+32*i` のlane-striped版をload/update/store全体で作り、GPU counterとT=1/8の依存チェーンで比較する

fastmlx/kernels/gated_delta_states.py:9-21,28-31,139-200; pyproject.toml:9-10 / 怪しい / `mlx_lm.models.gated_delta.compute_g`、コピーしたkernelのshape/mask/state semantics、Metal template/grid/threadgroup契約に直接依存する一方、依存指定は下限だけで上流変更を受け入れる / `_mlx_compat.py` にrevision・signature・shape契約を集約し、MLX/mlx-lmの上限を固定してupstreamとの差分テストを置く

### fastmlx/kernels/qmv_wide_nocap.py

fastmlx/kernels/qmv_wide_nocap.py:239-279 / 怪しい / commit全体でproduction callerがなく、呼び出しは `bench/test_qmv_wide_nocap.py` だけだった。意図的な単体foundationの可能性はあるが、現状のSpecEngineではM=6..12でもこのkernelへ到達せず、A2を接続するdispatcher契約もない / QuantizedLinear差し替えまたはdispatch層へ接続し、実推論で経路選択を検証するintegration testを追加する

fastmlx/kernels/qmv_wide_nocap.py:77-118,186-200,271-275 / 改善 / sourceはMごと、Metal templateはさらにdtype/K/Nごとに特殊化されるため、複数モデル・複数layer shapeでは初回compile latencyとpipeline cacheが増える。実際のcompile時間・常駐memoryは要実測 / 対応shape表を限定してprewarm/計測し、A2では可能ならMを共有できるkernel構成と明示的cache policyを採用する

fastmlx/kernels/qmv_wide_nocap.py:58-74,139-170,204-235; pyproject.toml:9 / 怪しい / MLX 0.32.2内部のaffine 4bit nibble順、scale/bias dtype、SIMD_SIZE=32、`qmv_wide_impl` の分担方式を複製しているが、MLXの上限versionがない。packing変更時はeligibilityを通ったままsilent wrong resultになり得る / 対応MLX上限を固定し、各対応dtype/M/K/Nを `mx.quantized_matmul` と比較するversion contract gateを必須化する

### fastmlx/convert.py

fastmlx/convert.py:158-160,235,276-277 / 壊れる / `mlx_lm_load` はMTPを含む生checkpointと判定して本体RMSNormへ既に`+1`を適用する。そのsanitized本体へ `mtp.*` を再結合して保存するため、出力再ロード時にmlx-lmが再び「MTPあり」と判定し、本体normへ二度目の`+1`を適用する。付属テストはNaN/Infしか見ない / 保存時だけ本体のshift対象normをraw規約へ`-1`して、source-loaded対output-reloadedのnorm値・logit一致テストを追加する

fastmlx/convert.py:112-144,268-277 / 怪しい / 保存済みMTPを読む `load_quantized_mtp` は存在するがproduction経路から呼ばれず、`fastmlx/cli.py:36-40` は変換artifactではなく元checkpointからMTPを再抽出する。MTP同梱artifact単体では通常CLIを起動できない / configの`fastmlx_mtp`を検出して同梱MTPをproduction loaderから読み、元snapshot依存をなくす

fastmlx/convert.py:88-100,237-248 / 壊れる / `dry_run_layers>=実layer数` ではmodelを切らない一方、configには指定値をそのまま書くため、例えば65指定で64層weightと65層configを出力する。`full_attention_interval`未満も`fa_idx`参照で壊れる / `full_attention_interval <= dry_run_layers <= len(layers)` をconfig変更前に検証する

fastmlx/convert.py:207-209,232,258-274,299-312 / 怪しい / CLIが任意のgroup_size/bitsを受け入れるが、MLX affineの対応値はgroup 32/64/128・bits 2/3/4/5/6/8に限定される。base側は非整除layerをskipする一方、MTP側は全Linearを無条件量子化し、0指定時は実際のdefault値と記録metadataもずれる / 対応値と全MTP Linearの入力次元整除をpreflightし、metadataにはmoduleが採用した実効group_size/bitsを書く

fastmlx/convert.py:33-43,96-99,235,266; pyproject.toml:10 / 改善 / `qwen3_5.TextModelArgs`、`model.language_model.model.layers`、`mlx_lm.utils` の `load/quantize_model/make_shards/save_config/create_model_card/get_total_parameters`、Qwen sanitizeとMTP key命名へ直接依存し、mlx-lmは下限しか固定されていない / converter用compat adapterへimport・sanitize revision・config schemaを集約し、対応上限とconvert/reload contract testを追加する

### fastmlx/spec.py

fastmlx/spec.py:53-67,70-108 / 怪しい / capture時は計算済み`ssm_mask`を渡さず、upstream GDNが行うqkv masking、length-aware conv cache更新、masked recurrence、`ArraysCache.advance(S)`を省略する。現行B=1 chatでは休眠しているがprepared/padded batchではnative forwardと状態が分岐する / `_linear_capture`へmaskを渡し、upstreamのmask・length・advance処理を再現したB>1 differential testを追加する

fastmlx/spec.py:70-112 / 壊れる / manual captureはupstream GDNの`sharding_group`入力`sum_gradients`と出力`all_sum`を省略しているため、sharded modelのverify経路だけhiddenが不正になる / upstream同様の分散前後処理を追加するか、capture入口でsharded modelを明示的に拒否する

fastmlx/spec.py:117-122 / 怪しい / rollback対象を`isinstance(c, KVCache)`へ固定しており、別のtrimmable cacheや将来のsubclass構成ではverifyで追加したtokenをtrimしない。現行Qwen cacheでは成立するがversion/model変更に脆い / `is_trimmable()/trim()`のcache protocol経由にし、非対応cacheはsilent継続せず拒否する

fastmlx/spec.py:180-188,193-214,257-332,337-341 / 壊れる / reuse時はsession cacheをaliasしたままcommit前に更新するため、model error・KeyboardInterrupt・`on_tokens`例外で「更新済みcache＋古いprocessed/h_last」がsessionに残り、次回suffixを二重投入する / reuse開始時にsessionをinvalidateするか、offset・linear stateをsnapshotして例外時にrestoreし、成功時だけ全fieldを一括publishする

fastmlx/spec.py:204-216,313-318,337-341 / 怪しい / n_draft/max_draftをoffにしたturnではMTP cacheを更新しないのにprocessedは前進するため、`0→on`または`on→0→on`でMTP KV履歴に欠落が生じる。最終token correctnessはverifyで守られてもdraft品質とsession invariantが壊れる / MTP cacheのvalidity/modeをsessionへ保存し、欠落後の再enableではMTPをfull rebuildするかoff中もmaintainする

fastmlx/spec.py:197-217,231-252,291-329 / 壊れる / `max_tokens`はmulti-token stepの前にしか確認されず上限を超えて返せ、`max_tokens=0`でも初期tokenを1個返す。accepted draft中にEOSがある場合も、表示はEOSで止める一方でrollback/MTP/fed_genはEOS後のtokenまで保持する / 初期sample前にbudgetを処理し、残budgetに合わせてdraft windowを制限し、EOS位置でeffective consumed・rollback・MTP append・fed_gen・h_lastを揃える

fastmlx/spec.py:204-216,228-252 / 怪しい / `n_draft=0,max_draft=0`でもdefault `lookup_len=16`のlookup draftは動くため、n_draft=0が非投機baselineを意味するかは意図不明であり、states_allを伴うmulti-token verifyも発生し得る / n_draft=0の契約を明示し、完全offならlookupもgateする。MTPだけoffの意味なら別名/flagに分け、baseline testでは`lookup_len=0`を固定する

fastmlx/spec.py:18-21,40-129,178-216,233-318; pyproject.toml:9-10 / 改善 / mlx-lm内部依存は `create_attention_mask/create_ssm_mask`、`KVCache`具象型と`offset/trim`、`model.language_model/.model/.args/.lm_head`、`embed_tokens/layers/norm/fa_idx/ssm_idx`、layerの`is_linear/linear_attn/norms/mlp`、GDN全projection・conv/head寸法・`A_log/dt_bias/norm/out_proj`、`ArraysCache[0]/[1]`配置、`make_cache`、MTP cache/norm規約に及ぶが、互換層も上限pinもない / `_mlx_compat.py`へ全attribute/signature/cache contractを集約し、対応MLX/mlx-lm上限とstartup contract testを追加する。A2の単体kernel実験は分離できてもA3/B1統合・identity gateはこの互換性修正後に行う

