# 段の現在地 (2026-09-02 更新。**まずここを読む**)

| 段 | 状態 | 根拠 |
|---|---|---|
| 段 1 QSA を切る | **決着** | 実測済み |
| 段 2 indexer の内訳 | **決着** | `tools/micro_indexer.py` |
| 段 3 attention を下限へ | **決着** | gather attention 実装、prefill では辞退 (`aa0c6d6`) |
| 段 4 GDN 連結射影 | **決着 (却下)。ただし理由が違った** | decode は中立 (-0.0%)、**prefill が +70.2%**。旧記録の +495% は測り方の誤り (下記) |
| 段 5 MoE の診断 | **決着** | 相手のカーネルは `B*S==1` 限定。差は融合数と K=640 |
| 段 6 未帰属 4.89ms | **決着 (存在しない)** | 本番経路 (段階投入あり) に対する残差は 3 run とも **+0.45〜+1.12ms で符号が正**。4.89ms は「段階投入をしなかった場合の隙間」だった (下記) |
| **段 P0 prefill の解剖** | **決着** | `tools/prefill_anatomy.py`、3 チャンク実測 |
| 段 P1 prefill attention | **決着 (格下げ)** | attention は 9-30%、効率 65-77%。融合カーネルは -0.9% |
| **段 P2 prefill の MoE** | **決着** | 伸びしろは 8.9s ではなく **1.4-1.7s** (会計漏れ 0.6s + タイル水増しの転嫁後)。G=4 が谷底、K=640 は無罪 |
| 段 P3 prefill の GDN | **決着 (ブロック化は負け)** | 取り分 4.3s は**私の見積もり誤り**。スキャンは GDN の 20% で取り分は約 1s。実装して 1.68 倍遅い (下記) |
| 段 P4 HC と PLE の下限 | **決着** | HC 実測 3.5s / 下限 1.87s、PLE 0.20s / 0.10s。**PLE は無罪** |
| 段 P5 indexer を分ける | **決着** | prefill では attention の 6%。**decode の 43% とは逆で、犯人ではない** |
| 段 C 較正プロファイル | **決着 (式は 2 勝 2 敗)** | `tools/calibrate.py` + `mlxturbo/calibration.py`。下記 |
| 段 X1 pooled 増分キャッシュ | **決着** | 採用済み |
| 段 X2 STAGE_EVERY | **決着 (既定が最良)** | 1=+0.6%/+0.3%、2=基準、4=+1.1%/+0.6%。1 を測ったら悪化で、2 が谷底 |
| 段 X3 スコアボード再計測 | **決着** | 下記 |
| G1 幅掃引 | **決着** | M=8 で +138.6% → **木構造投機は作らない** |
| G2 受理トレース卓上計算 | **不要になった** | G1 で取り分が消えた |
| G3 同時実行の比較 | **決着 (測る土俵に立てていない)** | A-1 で長さ上限は外れ、`wait_ms=1000` で発火もする。**が、噛んでも壁時計が変わらない** (バッチ経路に単独経路の最適化が無いため)。移植が先 |

## PREFILL_GROUP は G=4 で確定 (2026-09-02)

`--knob prefill-group --variants 2,4,8` (下駄を取ったハーネス、17k、prefill_s):
**G=2 +3.1% / G=4 基準 / G=8 +0.6%。**tok/round は 3 条件とも完全一致。

**保留になっていた「G=8 で -1.1% の兆候」は再現しなかった。**あれは下駄を
履いたハーネスで取った数字。G=2 が明確に悪いので行数の効果はあるが、
**4 で頭打ち。**逆量子化を薄める側の手はここで終わり。
**MoE の残りは K=640 の `qmv_fast` 落ちに絞られる。**

## 対 mlx-serve の再計測 (2026-09-02、段 X3)

ハーネスを 3 箇所直したうえでの実測 (`bench/results/vs-mlx-serve-0902.json`)。

| 文脈 | 冷 TTFT serve / turbo | 温 TTFT serve / turbo | tok/s serve / turbo |
|---|---|---|---|
| 0 | 0.05 / 0.36 | 0.72 / 1.04 | 54.6 / **54.6** |
| 4k | 5.73 / **7.00** | 0.85 / **1.10** | 56.1 / **48.0** |
| 17k | 21.6 / **30.0** | 0.91 / **1.25** | 47.8 / **43.8** |
| 50k | 58.4 / **失敗** | 2.26 / 失敗 | 42.3 / **0** |

**50k を通した版** (`--ngram ~/models/ddalcu-ngram` = interleaved、RAM 0。
`bench/results/vs-mlx-serve-0902-inter.json`):

| 文脈 | 冷 TTFT serve / turbo | 温 TTFT serve / turbo | tok/s serve / turbo |
|---|---|---|---|
| 0 | 0.05 / 0.35 | 0.76 / 1.12 | 52.0 / 51.9 |
| 4k | 5.89 / **8.11** | 0.89 / **1.16** | 49.0 / **46.3** |
| 17k | 22.2 / **31.7** | 0.91 / **1.30** | 50.4 / **44.4** |
| **50k** | 58.1 / **102.4** | 2.19 / **1.72 (勝ち)** | 41.2 / **42.9 (勝ち)** |

**50k で温 TTFT と decode が逆転する。**長文脈ほど疎性 (QSA / gather) が効く
という読みが実測で出た形。一方 **50k の冷 TTFT は 102 対 58 で負け幅が最大**で、
段 P0 の「MoE 効率 47.6%、伸びしろ 8.9s」と同じ場所を指している。

**interleaved は decode をほとんど犠牲にしていない** (17k で 44.4 vs
separate の 43.8。プロセスをまたぐので数 % は誤差だが、32GB 払うほどの差は
見えない)。**既定を interleaved にすべき**、というのが手持ちの根拠から出る結論。

**全項目で負けている。**記録されていたスコアボード (短 +8% / 17k 同着 /
4k -4.5%) とは合わない。**今日ハーネスを 3 回直したので、古い数字の方を疑う。**

直したのは (a) `model` にプレースホルダを送って 404、(b) 思考
(`reasoning_content`) を数えず長文脈が nan、(c) 温めに測定と同じプロンプトを
使って「冷」が冷えない、の 3 点。

**50k はメモリ不足で落ちる** (`[METAL] Insufficient Memory`)。モデル 91GB +
n-gram の RAM テーブル 32GB = 123GB / 128GB で、残り 5GB に 50k の KV と
活性が入らない。**性能ではなく構成の問題。**サーバーは 200 を返して
31 秒後にストリームへエラーを流す (黙って固まってはいない)。

---

# decode と prefill を下限まで詰める

前半が **decode (帯域律速)**、後半が **prefill (FLOP 律速)**。下限の出し方が
違うので分けてある。

# やることの現在地 (2026-09-01 時点。ここが最新)

決定済みの方針: **後追い 4 本は全部実装する** (ユーザー決定)。そのうえで
**木構造投機を新しいレーンとして立てる**。ただし木は最も高いので、
**安い判定を 2 つ通してから着手する。**

## 判定 (安い。これが他の順番を決める)

| # | やること | GPU | 何が決まるか |
|---|---|---|---|
| G1 | verify 幅 M ∈ {2,4,8,16} の ms/round 掃引 | 要 | **木の前提**「dispatch 律速なら幅は安い」が成立するか。同時に **MoE の直し方** (`SORT_MIN=16` の谷に verify 幅が乗っているか) |
| G2 | 実受理トレースから木 (ノード 8-16) の期待 tok/round を卓上計算 | 計測 1 回 | 木の取り分。鎖の depth 適応比 **+0.3 tok/round 未満なら木は作らない** |
| G3 | 同時 N 要求での対戦比較 (`bench/vs_mlx_serve.py`) | 要 | **相手の専用カーネルは `B*S==1` でしか発火しない。**同時実行下で非対称が出るなら、畳んだバッチ×投機レーンの評価が変わる |

**G1 で M=8 の ms/round が +25% 以上伸びたら、木の取り分は消える** (fable の
反転条件)。G2 が +0.3 未満でも同じ。**両方通ったときだけ木を作る。**

G3 は木とは独立。安くて、当たれば位置づけが変わる。

## G1 決着 (2026-09-01): 幅は安くない。木は作らない

`tools/decode_ab.py --knob depth --variants 1,3,7,15 --only long --ctx 17000
--prefill-once` (幅 M = depth+1、prefill 1 回使い回し、回文順で順序バイアス相殺)。

| 幅 M | ms/round | tok/round |
|---|---|---|
| 2 | 39.6 | 1.678 |
| 4 | 56.2 (+41.9%) | 2.195 (+30.8%) |
| **8** | **94.5 (+138.6%)** | 2.325 (+38.6%) |
| 16 | 156.4 (+295.0%) | 2.335 (+39.2%) |

**反転条件が発火した。**宣言は「M=8 で ms/round が +25% 以上伸びたら幅は安く
ないと判断する」で、実測は +138.6%。桁で外している。

**さらに tok/round が飽和する。**M=4 → M=16 で幅を 4 倍にして +6.4% しか
伸びない。fable が挙げた「木は対数的にしか伸びない」がそのまま出た形。
**費用 3 倍に対して取り分 6%。**G2 (受理トレースの卓上計算) を待つまでもなく
木の取り分は消えた。

**結論: 木構造投機は作らない。**分布保証つきの多 draft 厳密再帰棄却という、
このレーンで一番高い実装を、測定 1 本で回避できた。

**副産物 1: 現行の既定は正しい。**17k では ms/tok が depth 1 で最小
(23.6ms、depth 3 は +8.5%)。`_depth_ctx_limit` が indexer_budget 超えで
depth 1 に落とす既定の挙動が、実測で裏付けられた。

**副産物 2: MoE の設計判断 (K6) にも答えが出た。**M=4 (添字 40 本) から
M=8 (80 本) で ms/round が +41.9% → +138.6% と跳ねる。top_k=10 なので
M=8 はちょうど `total_inds >= 64` / `SORT_MIN=16` の境界をまたぐ。
**経路が切り替わる谷が実測に見えている。**

## K1/K2 決着 (2026-09-01): どちらも負け。MoE の共有タイルは畳む

**K2 MoE (`MLXTURBO_MOE_VERIFY`) — 反転条件が発火した。**宣言は「同期を消しても
in-model で負けるなら、今度こそカーネルの出来の問題として畳む」。

| | 短文脈 | 長文脈 |
|---|---|---|
| ms/tok | **+15.9%** | **+13.4%** |
| ms/round | — | +11.1% |
| tok/round | **-3.8%** | **-1.9%** |

**遅いだけでなく受理率まで落ちる。**ホスト同期 (`.tolist()` x3) の除去も、
パディング threadgroup の早期 return も、in-model の負けを覆さなかった。
micro でも本家 `mx.gather_qmm` の 233.8us に対し 301.8us。

**「共有タイル v2 が負けたのはホスト同期のせいかもしれない」という仮説は棄却。**
差の出どころは fable のコード実読どおりで、(a) そもそも帯域律速ではない
(本家の読み出し 27MB は 300GB/s なら 90us、実測 234us なので起動・レイテンシ
律速)、(b) 68us のギャップは `_segments_gpu` の 15 本前後の直列小カーネル。
v1 (ホストでセグメント計算、計測ループ外) が 230.3 vs 234.6 で同着だった記録と
整合する。**前処理を外に出しても上限は同着。**

**これで MoE の共有タイル機構は打ち止め。**`docs/BACKLOG.md` の残件 12
(`_segments_gpu` が層あたり 2 回) は、この機構を使わないので直さない。

**K1 HC 書き戻し (`MLXTURBO_HC_WRITE`) — 長文脈で負け。**

| | 短文脈 | 長文脈 |
|---|---|---|
| ms/tok | -0.6% | **+5.2%** |
| tok/round | 一致 | 一致 |

tok/round は完全一致なので純粋に費用の話。**2 op を 1 に畳んで遅くなる**のは
説明が要る。仮説: `mx.compile` は入力の形ごとにキャッシュを引くので、
draft と verify で S が変わる長文脈では引き直しの費用が乗る (短文脈では
depth が 2 で形が安定し、長文脈では depth 1 に落ちて混ざる)。**未検証。**
既定 off のまま。採るなら形を固定するか、短文脈限定にする必要がある。

### 4 本そろっての所見

| | 削ったもの | 成果 |
|---|---|---|
| HC 書き戻し | 192 -> 96 dispatch | 短 -0.6% / 長 **+5.2%** |
| GDN 前処理 | 8 -> 1 dispatch x 36 層 | -0.9% (発火確認は取り直し中) |
| MoE | ホスト同期 48 回/ラウンド | **+13〜16%** |
| prefill attention | gather + 密 sdpa を 1 本に | prefill -1.4% (S 下限を入れて取り直し中) |

**融合という手そのものが、この系ではほとんど効いていない。**dispatch を
3 通りに削って全部 1% 前後か悪化。G1 の「幅を広げると +138%」と合わせると、
**decode は dispatch 律速ではない。**段 6 の未帰属 4.89ms を xctrace で
見に行くのが先で、カーネルを書くのはその後。

## HC の +4.5% は機構ではなく mx.compile だった (2026-09-01、3 変種)

`--knob hc-write --variants A,C,B` (A=融合 / **C=差し替えの機構だけ、compile は
通さない** / B=素)。長文脈 17k:

| | ms/round |
|---|---|
| A (融合) | **+4.5%** |
| **C (機構だけ)** | **-0.2%** |
| B (素) | 基準 |

**C - B はゼロ。差し替えの機構は無料。**+4.5% は `mx.compile` そのもの。

`DecoderLayer._combine` は **2 op を 1 に畳むより、2 op のまま出した方が速い。**
`mx.compile` は呼ぶたびに入力の形で引き当てをするので、層あたり 2 回 x 48 層 x
ラウンド数ぶんのその費用が、融合で浮く 96 dispatch を上回る。

**一般則として持ち帰る: 小さい elementwise の式を、形が変わりうる呼び出しで
高頻度に叩く場所に `mx.compile` を置かない。**

### 「機構が on 側を罰している」仮説は、HC については棄却

ただし **`gdn_prework` の +5.3% は別原因。**あちらは発火カウンタが 0 のまま
遅く、`patched` の中の `eligible()` の評価 (層ごと・フォワードごとに 20 条件、
`mx.default_device()` と `mx.metal.is_available()` を含む) が犯人。HC の
`patched` に `eligible()` は無い。**同じ桁だったのは偶然。**

| | 原因 | 性質 |
|---|---|---|
| HC +4.5% | `mx.compile` の引き当て | 融合そのものの費用 |
| GDN +5.3% | `eligible()` の評価 | 機構の費用 |

**`rms_norm_gated` (空振り記録) と `moe_route` (純損 +0.34ms) は GDN と同じ側**
(`patched` の中で `eligible()` を評価する) なので、3 変種での再審査は続ける。

## 後追い 4 本 (実装中)

| # | やること | 状態 |
|---|---|---|
| K1 | HC 書き戻しを 1 dispatch に (192→96) | **実装済み** (`940eebd`)、A/B 待ち |
| K2 | MoE のホスト同期除去 (`.tolist()` x3) | 実装済み、実モデル確認待ち |
| K3 | GDN 前処理の融合 (8 dispatch → 1) | 実装中 |
| K4 | prefill attention の融合カーネル (段 P1) | 実装中 |
| K5 | HC 遅延畳み込み (96 → 0) | K1 の A/B の後 |
| K6 | MoE の融合数削減 + K=640 の qmv_fast 落ち | K2 の後。**G1 の結果で設計が変わる** |
| K7 | GDN 状態 bf16 | 速度 A/B と KLD をセットで。K3 とは独立 |

## 木構造投機 (G1 と G2 を通ったら)

**実装の本体は速度ではなく分布保証。**素朴な木 + 受理は分布を動かすので、
多 draft の厳密再帰棄却 (SpecTr 系) が要る。ここを緩めたら
「品質を売らない」という看板と矛盾するので、**緩めた実装は作らない。**

既知の障害:

- **GDN の再帰状態は木と相性が悪い。**KV キャッシュと違い分岐ごとに複製か
  再走が要る。`spec_flash.py` の rollback 用 GDN 転記が既にその兆候で、
  簿記コストが分岐数に比例する
- tok/round は幅に対数的にしか伸びない。MTP ヘッドの深部品質が低ければ
  2.0 → 3.0 に必要なノード数は 16-64 に膨れ、そのとき verify はもう安くない
- MoE の expert union は幅に線形に増える帯がある (飽和点は未測 = G1)

**MTP 自体は売りにならない** (相手も持っている。draft rerank に至っては
相手の設計の移植)。木にして初めて差になる。

## レビュー

後追い 4 本がそろったら fable に 1 回、範囲を絞らずに通す (ユーザー指示)。
重点は **3 者が同時に触った `_vendor/qwen4_exp.py` のシーム** —
3 つの呼び手 (`batch.py` / `batch_spec.py` / `spec_flash.py`) を外していないか。

---

# decode を帯域まで詰める (MoE / GDN / attention / indexer)

2026-09-01 の解剖 (`tools/decode_anatomy.py`) と帯域下限の突き合わせで、
**decode の 4 部品が全部、達成帯域 393GB/s の下限に対して 18-50% しか
出ていない**ことが分かった。ここはその 4 つを順に詰めるための作業計画。

数字は 17k・width=2 のフォワード 1 回 (壁時計 40.83ms)。1k と比べると、
**文脈長で伸びるのは attention だけ** (MoE +0.6ms、GDN +0.1ms、HC +0.05ms)。

| 部品 | 実測 | 帯域下限 | 効率 | 卓上 | 状態 |
|---|---|---|---|---|---|
| MoE 48 層 | 12.01 ms | 6.0 ms | 50% | **6.0 ms** | カーネル 3 敗 |
| attention (indexer 除く) | 7.59 ms | 1.90 ms | 25% | **5.7 ms** | 未着手 |
| indexer (QSA) | 3.80 ms | 0.13 ms | 3% | **3.7 ms** | 未着手 |
| GDN 36 層 | 6.56 ms | 3.25 ms | 50% | 3.3 ms | 連結射影が未測 |
| HC 97 回 | 4.01 ms | ~0.9 ms | ~23% | 3.1 ms | 融合済み |
| lm_head | 1.98 ms | 0.90 ms | 46% | 1.1 ms | **未解決の謎あり** |
| **未帰属** | **4.89 ms** | — | — | — | 下の注記を読むこと |

**下限の出し方** (達成帯域 393GB/s、実効 4.47bpw = 0.559 B/重み):

| 部品 | 内訳 |
|---|---|
| MoE | (union ~16 専門家 x 3 行列 x 2560x640 + 共有 + router) x 48 層 = 2.4GB |
| attention | KV 414MB (12 層 x 16852 x 2head x 256 x 2 x 2B) + 射影 334MB |
| indexer | 生鍵 52MB (12 層 x 16852 x 128 x 2B) |
| GDN | 重み 1.17GB + 再帰状態 112MB (**書き戻しは数えていない**ので下限は甘め) |
| HC / lm_head | 低ランク重み ~356MB / 語彙 248320 x 2560 = 355MB |

**MoE の union は推定**。実測は「3 トークンで top-10 の union が 21-22/30」で、
2 トークンぶんは内挿した ~16/20。ここは `tools/expert_stats.py` で実測できる
ので、段 5 の最初に測ること。**下限が動けば効率も卓上も動く。**

**「未帰属 4.89ms」を「ディスパッチの隙間」と決めつけないこと。**説明は 2 つ
あって、区別できていない:

1. カーネル間の隙間 (グラフ構築中の GPU 遊休)
2. **単体計測が重なりを再現できていない** — 部品を 1 つずつ呼ぶと、本番で
   重なっているぶんが分離されて和が小さく出る

しかも `tools/decode_anatomy.py` の壁時計は `model(...)` を直接呼んでいて、
**本番の decode が通る段階投入 (`_staged_forward`) を通っていない**。
つまりこの 4.89ms は「段階投入なしの隙間」で、本番の隙間はこれより小さい。
段 6 に着手する前に、まず壁時計側を `_staged_forward` に揃えて測り直すこと。

---

## 段 1 の結果で、あとの形が決まる

**この計画は QSA の 1 実験を起点にした分岐**になっている。段 2 と段 3 の
中身は段 1 の結果で入れ替わるので、順番を飛ばすと無駄になる。

```
段 1: 17k で QSA を切って measure (--knob qsa)
 │
 ├─ (a) 速くなった × 品質も持つ (KLD 現行比 +0.0005 以内)
 │      → 長文で QSA を切る/budget を上げるのが答え。5ms 級を回収
 │      → 段 2 (indexer の内訳) と B4 (pooled 増分キャッシュ) は **不要**
 │      → 残るのは 段 3'(素の causal を下限へ) / 段 4 / 段 5 / 段 6
 │
 ├─ (b) 速くなった × 品質が持たない
 │      → **QSA は要る。だが「マスクが重い」という仮説は当たり。**
 │      → 段 3 の gather 案 (選ばれたブロックだけ集めて dense sdpa) が本命。
 │        疎性を sdpa に伝えれば、品質を保ったまま同じ 5ms を取れる
 │      → 段 2 は後回し (indexer 自身より、その使い道の方が高い)
 │
 └─ (c) 速くならなかった (3% 以内)
        → 仮説が外れ。attention 本体の 18% は加算マスク以外の理由
        → 段 2 (indexer の内訳) を通常どおり実施し、attention は
          別の容疑者を探し直す (sdpa のカーネル選択、KV レイアウト)
```

(a) と (b) は**どちらも「マスクが重い」の裏付け**で、違うのは品質だけ。
(c) だけが仮説の否定になる。つまり段 1 は「当たり/外れ」ではなく
**「どこを直すかを決める」**実験。

段 4 (GDN)・段 5 (MoE)・段 6 (隙間) はこの分岐と独立に走らせてよい。

---

## 順序 (取り分 ÷ コスト で並べた)

**先に測る。カーネルから入らない。**3 敗はどれも「micro で勝って in-model で
負けた」ので、まず「なぜ素の実装が下限に届かないのか」を突き止める。

### 段 1. QSA を切ったらどうなるか (取り分 5ms 級、コスト 30 分)

**仮説**: `mx.fast.scaled_dot_product_attention` は加算マスクを渡されても
**全 KV を読んで全スコアを計算する**。つまり QSA の疎性は sdpa 側の節約に
なっていない。だとすると長文の QSA は「indexer の 3.80ms を払って、
attention は 1 ミリも軽くなっていない」ことになる。

attention 本体が下限の 18% しか出ていないのも、16852 列ぶんの加算マスクを
実体化して足す費用で説明が付く可能性がある。

**やること**: `tools/decode_ab.py --knob qsa` (実装済み)。B は
`indexer_budget` を巨大にして早期 return に落とす = 素の causal。

**判定**: 17k decode の ms/token。**出力は変わる** (QSA は full attention の
近似なので、切ると近似が外れる方向 = 品質は上がりうる) ので、速くなったら
**KLD と受理率を測ってから**採否を決める。

**行き先は上の分岐図のとおり** (a) 速い×品質OK / (b) 速い×品質NG /
(c) 速くならない の 3 つ。**反転条件**: (c) = 速度差が 3% 以内なら仮説が外れ。

### 段 2. indexer の内訳を割る (取り分 3.7ms、コスト 半日)

**行き先 (c) のときに実施する。**(a) なら不要 (QSA ごと消える)、(b) なら
後回し (indexer 自身より、疎性を sdpa に伝える段 3 の方が取り分が大きい)。

**内訳の候補**:

| 中身 | 17k での規模 | 増分化できるか |
|---|---|---|
| pooled の作り直し (mean + k_layernorm) | 生鍵 52MB を毎回読み直し、4213 ブロック x 12 層 | **できる** (確定したブロックは不変) |
| pooled への rope | 4213 x 128 x 12 層 | できる (同上) |
| einsum q·pooled | 24 head x 4213 block x 128 x 12 層 | できない |
| argpartition (top-k) | 4213 ブロックの部分ソート x 12 層 x 2 位置 | **できない** (クエリごとに違う) |
| keep マスクの構成 (repeat + concat) | 16852 列 x 2 位置 x 12 層 | できない |

**やること**: 上の 5 つを個別に計時する micro を書き、どれが 3.8ms の主体かを
出す。**pooled が主体なら B4 (増分キャッシュ)、argpartition が主体なら
別の設計** (ブロック数を減らす = compress_ratio を上げる、あるいは
running top-k)。

**反転条件**: 主体が argpartition で、かつ MLX の argpartition が既に
帯域律速なら、ここは畳んで段 3 へ。

### 段 3. attention 本体を下限に寄せる (取り分 6.2ms、コスト 数日)

**行き先で中身が変わる。**

- **(a) QSA を切れる場合 — 段 3'**: 素の causal sdpa で 17k を読む形になる。
  下限 1.05ms に対して実測がいくらかを測り直し、そこからの改善を考える
  (このとき加算マスクは消えているので、残る差は sdpa 自体の効率)。
- **(b) QSA が要る場合 — 段 3 本命**: 疎性を sdpa に伝える経路を作る。
  加算マスクではなく**選ばれたブロックだけを gather してから dense sdpa**
  に渡す (token_budget=2048 なら 16852 → 2048 列 = 読み出しが 8 分の 1)。
  gather の費用 (2048 列 x 12 層の KV コピー = 50MB) を払っても 414MB を
  読むより安い。**MLX の op だけで書ける** (カーネル不要)。
  **出力はビット一致しない** (softmax の対象集合は同じだが加算順が変わる)
  ので、判定は tok/step + KLD。
- **(c) 仮説が外れた場合**: 容疑者を探し直す。候補は sdpa のカーネル選択
  (`docs/research/SDPA-WIDTH-WALL.md` の壁は S=2 では効かないはずだが、
  マスクの型で経路が変わる可能性)、KV のレイアウト、GQA の展開。

**判定**: 17k decode の ms/token + tok/step + KLD。**ビット一致は要求しない** --
(b) の gather 案は softmax の対象集合こそ同じだが加算順が変わるし、(a) は
そもそも別の注意を計算している。
**反転条件**: gather の費用が読み出しの節約を食ったら (実測で 3% 以内なら)
畳んで、そこで初めてカーネルを考える。

### 段 4. GDN の連結射影 (取り分 3.3ms のうち一部、コスト 30 分)

**単独 A/B の記録が無い。**既定 off の理由は「連結で N が変わると qmv の
カーネル変種が変わり、加算順の違いが最終 ulp を動かす疑い (tok/step
2.44 -> 2.23 の低下と時期が一致)」。**時期が一致しただけで犯人扱いされた
可能性がある。**

**やること**: `tools/decode_ab.py --knob wide` (実装済み)。
**判定**: ms/token と tok/round の両方。tok/round が落ちるなら疑いは本物。
**反転条件**: tok/round が 3 プロンプト平均で 2% 以上落ちたら既定 off のまま。

### 相手の GDN を読んだ (2026-09-01、`~/dev/mlx-serve`)

**連結射影そのものは差ではなかった。**相手も既定は qkv/z/b/a を別々に
`qmatmul` で叩いている (`transformer.zig:16016-16018`, `16558-16559`)。
段 4 の掃引は「うちだけが損している」の検証ではなく、単独で得かどうかの検証。

**差は前後の融合で、しかも桁が違う。**

| | うち | mlx-serve |
|---|---|---|
| 再帰カーネル本体 | mlx_lm の移植 | 同じものの移植 + 特化 4 変種 |
| 前処理 | conv1d / silu / rms_norm x2 / スケール x2 / g / beta = **8 dispatch** | **1 カーネル** (`gdnPreworkFused`, `transformer.zig:21969-22165`、既定 on) |
| 後処理 | RMSNormGated 3 op 未融合 (専用カーネルは空振りで off) | **1 カーネル** (`gdnNormGateFused`, `transformer.zig:22279-22370`、既定 on) |
| 状態 dtype | fp32 強制 (`kernels/gated_delta_states.py:239`) | **bf16** (`transformer.zig:16209`) |
| conv1d | MLX 標準 op | decode 幅では prework 内に 4 タップ手書きで吸収、**op ごと消える** |
| decode/prefill の経路 | 同一 | 分岐 (`gdnBlockedEligible`: seq>=64 かつ Dk==128 で blocked-seq。**decode 幅は非該当**) |

再帰本体を除いて、うちは 11-12 dispatch/層、相手は 3。それが 36 層。
`mlxturbo/fused.py` 自身が「Flash-Next の decode は dispatch 律速」と書いて
いる前提に照らすと、GDN が下限の 50% に留まる相当部分はこれで説明が付く。

**RMSNormGated の「空振り」は読み直しが要る。**うちの `enable_rms_norm_gated()`
は 3 op を 1 に畳む**単独差し替え**で、実測で空振りだった
(`runner.py:1156-1157`)。相手の同等機能は**単独では発火しない** — prework 融合
が成立したときだけ動く (`transformer.zig:16345-16350`)。前後に 9 dispatch が
残ったまま 3 を 1 にしても効かないのは、dispatch 律速の系では自然。
記録は「この融合は無意味」ではなく「**単独では足りない**」と読み替える。

**状態 dtype は速度と品質の両方に触る。**fp32 3.1MB/層 → bf16 1.55MB/層。
再帰カーネルは毎ラウンド state_in/state_out を全読み全書きするので、36 層 x 2 で
~223MB/ラウンドが ~112MB になる (393GB/s なら 0.28ms/ラウンド)。ただし
**再帰の累積器を bf16 に落とす**話なので、KLD を測らずに入れない
(受け入れ幅 +0.0005)。相手がやっているから安全、とは言わない。

**やること (dispatch を削る側が先。取り分が桁で大きい)**:

1. GDN 前処理の融合カーネル (conv1d + silu + q/k の rms_norm + スケール +
   conv 状態更新 + g + beta を 1 つに)。decode 幅限定でよい
2. 1 が入った状態で RMSNormGated を再測 (空振りの再審査)
3. 状態 bf16 は速度 A/B と KLD をセットで。1・2 とは独立に測る

**反転条件**: 1 を入れて in-model の GDN 部品時間が 10% 以上縮まなければ、
「dispatch 律速」という前提そのものを疑い直す (xctrace で泡を見る)。

### 段 5. MoE の診断 (取り分 6.0ms、コスト 数日〜)

**カーネルから入らない。**3 敗 (moe_glu v1-v3、共有タイル v2、moe_route) は
どれも「micro で勝って in-model で負けた」か「単に遅かった」。今度は
「なぜ**素の** `gather_qmv_fast` が下限の 50% なのか」から始める。

**測ること**:

1. decode 幅の実形状 (M=2、E=512、union ~16、K=2560/640) で
   `gather_qmv_fast` / `gather_qmv` の単体帯域
2. **同じバイト数を密の `quantized_matmul` で読んだときの帯域**
3. 差が gather の添字コストか、MLX の 4bit qmv 自体の天井か

**分岐**:

- 密が 90% 出て gather が 50% → **添字が犯人**。専門家の重みを連続領域へ
  集めてから密 qmm に渡す案が成立しうる (コピー費用と要相談)
- 密も 50% → **MLX の 4bit qmv 自体の天井**。彼らのカーネルが 1.4 倍速い
  という既知の事実と一致し、ここは「彼ら級の qmv を書く」多日レーンになる

**この段だけは、結論が「多日レーン」でも構わない。**そのときは何を書くべきかが
確定するのが成果。

### 決着 (2026-09-01、micro 実測 + 相手のソース読解)

**micro の結果** (`tools/micro_moe_gather.py`、decode 幅 M=2、union 16、4bit gs=64):

| | us | GB/s | 下限比 |
|---|---|---|---|
| gate/up (K=2560->N=640) gather | 54.1 | 242 | 61.6% |
| gate/up 密 | 36.8 | 356 | **90.6%** |
| down (K=640->N=2560) gather | 94.3 | 139 | 35.4% |
| down 密 | 59.6 | 220 | **56.0%** |

**gate/up は密なら 90% 出る**ので MLX の 4bit qmm 自体は良い。gather の添字が
1.47 倍の費用。**down は密でも 56% しか出ない。**

**相手のソースを読んで分かったこと** (`~/dev/mlx-serve/src/transformer.zig`):

1. **相手の専用カーネルは `B*S==1` (純粋な単一トークン decode) にしか効かない。**
   投機の verify 幅 (B*S>1、M=2..4) では**相手も MLX の `gather_qmm`/`gather_mm`
   を使っている** (`do_sort = B*S > 1 || total_inds >= 64 || has_expert_bias`)。
   **うちの主戦場では op は同じ土俵。**カーネルの有無ではなく、その手前の差。
2. **融合の数**が違う。相手は decode で最大 **2 dispatch** (gate+up+SwiGLU 融合、
   **down + ルータ重み乗算 + top-K reduce まで融合**)。うちは gate/up/down が
   別々の 3 dispatch + reduce を `SparseMoeBlock.__call__` で別途。
3. **K=640 の扱い。**MLX の `gather_qmm` は `K%512!=0` だと "qmv_fast" の対象から
   外れて遅い汎用経路に落ちる (うちの `moe_verify_gather.py` のコメントに実測付きで
   書いてある)。相手のカーネルは K のブロック分割に非依存なので**最初から
   踏んでいない**。密 56% の説明になる。
4. **共有専門家は両者とも「別立てが勝つ」**に着地している (うちの
   `enable_moe_shared_fold` は verify +1.8ms で不採用)。ここは差にならない。

### 過去の敗北の原因が 1 つ特定された (共有タイル gather v2)

`mlxturbo/kernels/moe_verify_gather.py` は、呼び出し口で毎回
**`idx_sorted.tolist()` を叩いている** (379 / 575 / 761 行)。**これは
GPU->CPU の同期点**で、decode の毎トークン・毎層で発生する。相手の経路には
どこにも存在しない。

**共有タイル v2 が in-model で +46〜52% 遅かったのは、カーネルの出来ではなく
これかもしれない。**48 層ぶんの同期が毎ラウンド乗れば、その桁の差は出る。
「micro で勝って in-model で負けた」の典型的な形でもある。

**やること**: ホスト同期を消して測り直す (セグメント境界の計算を GPU 側に
移すか、同期を 1 ラウンド 1 回に畳む)。**これは負の結果の再審査**であって、
新しいカーネルを書く話ではない。反転条件: 同期を消しても in-model で負けるなら、
今度こそカーネルの出来の問題として畳む。

### 段 6. 未帰属の 4.89ms

部品和 35.95 に対して壁時計 40.83。1k では 26.57 に対して 32.68 で、
**未帰属は短文脈の方が大きい** (6.11ms vs 4.89ms)。

**着手の前に 2 つ片付ける** (上の注記):

1. `tools/decode_anatomy.py` の壁時計を `_staged_forward` に揃える。いまは
   段階投入なしの `model(...)` を測っていて、本番より隙間が大きく出る
2. 「隙間」と「単体計測が重なりを再現できていない」を分ける。分ける手は
   xctrace の GPU タイムライン (前回 7.3ms の泡を特定したのと同じ道具)

そのうえで `--knob stage-every` を回す (実装済み、コスト 30 分)。掃引が
16→2 で単調改善のまま端点で打ち切られていて **1 が未測**、しかも短 decode の
probe でしか測っておらず 17k は未測。

**HC の 23% の内訳が読めた (2026-09-01、相手のソース読解)。**読み側は既に融合
済みで相手と同格 (うち 2 カーネル `kernels/hyper_connection.py:183-368` 既定 on、
相手 3 カーネル `transformer.zig:23951-24127`)。**手を付けていないのは書き戻し側。**
`DecoderLayer._combine` (`_vendor/qwen4_exp.py:1299-1304`) は
`hyper + (x[..., None, :] * inject[..., None]).reshape(...)` を生の mx 演算で
呼んでいるだけで、`mx.compile` も専用カーネルも無い。attn/mlp で層あたり 2 回、
**96 回 x 2 op**。相手は `compiled_hc_write` の 1 カーネルにしたうえ、
decode/verify 幅 (<= 16 行、`HC_FUSED_MAX_ROWS`) では**書き込み自体を遅延**して
次の `hcRead` の N カーネルに畳み込み、**書き戻しの dispatch を 0 にしている**
(`transformer.zig:11538-11549`, `23979-23988`)。

**やること**: まず `_combine` を 1 カーネル化する (`mx.compile` で足りるか、
`hyper_connection.py` に 3 本目を足すか)。遅延畳み込みはその次。
**反転条件**: 1 カーネル化して in-model が動かなければ、96 回という回数自体が
効いていないということなので、HC は 23% のまま畳む。

**HC と lm_head もここに関係する。**どちらも下限の 23% / 46% で、合わせて
4.2ms が卓上にある。lm_head は「M=3 で 164GB/s、fast_qmm が適格なのに
in-model で負ける」という未解決の謎が既にあり (`docs/BACKLOG.md`)、
depth 既定が 1 に変わって M=2 になったので**前提そのものが変わっている**。

---

## 岐路の記録 (2026-09-01)

**問い**: 「相手のカーネルに追いつく」路線を主軸から降ろし、
「tok/round を上げる」路線に移すべきか。

**fable の判断**: 移すべき。理由は (a) decode は既に全帯 1.2 倍以内で後追いの
残額が薄い、(b) 相手は受理を緩めて depth 6-8 を買い KLD 3 倍を払っている →
うちの分布保証 + KLD 計測とセットなら真似できない看板になる、(c) エンドツー
エンドは既に決している (冷え 7.3 倍・prefill 3 倍・KLD 3 倍)。

**指摘された穴** (「dispatch 律速なら幅は無料」が成立しない条件):
MoE の expert union は幅に線形に増える帯があり、`SORT_MIN=16` / 相手の
`total_inds>=64` はまさに経路が切り替わる境界。飽和点は未測。tok/round は
幅に対数的にしか伸びない。GDN の再帰状態は分岐ごとに複製か再走が要る。
**分布保証つきの木 verify は自明でない** (素朴な木は分布を動かす。多 draft の
厳密再帰棄却が要る)。

**ユーザーの決定**: **4 本とも実装する。**「早くなるだろうし、どうせあとあと
やらなきゃいけない」。移行はしない。

**訂正**: `docs/POSITIONING.md:84-89` の反転条件は「1-4 を入れても decode が
1.2 倍以内に**詰まらなければ**降ろす」で、**発火していない。**全帯 1.2 倍以内に
詰まったので達成の側。一度「発火した」と誤記した。降りるとしたら理由は
「追いついたので追走の限界効用が薄い」であって「負けたから」ではない。

**それでも測る 1 つ**: verify 幅 M ∈ {2,4,8,16} の ms/round 掃引。これは
新軸のためだけでなく **MoE カーネルの設計判断そのもの** (SORT_MIN=16 の谷に
verify 幅が乗っているなら直し方が変わる)。

---

## 逆量子化を攻める案 (段 P0 の後。順番を守ること)

**先に段 P0。**記録されている 4bit qmm の天井は 11.2 TFLOPS で、17k prefill の
実効は 5.6 TFLOPS。**天井の半分しか出ていない。**逆量子化を速くしても、
当たっていない天井が上がるだけ。埋めるべきは 50% のギャップの方で、それが
段 P0 の中身 (部品和 19-20s に対し壁時計 32-38s、半分近くが未帰属)。

**この順番を守る理由**: 2026-09-01 に「attention が重いはず」と踏んで融合
カーネルを 4 本書き、全部 1% 前後だった。**測る前に書くと同じことになる。**

### それでも攻めるときの手 (取り分の大きい順)

1. **`MLXTURBO_PREFILL_GROUP=8` を確定させる。**逆量子化の費用は使い回す行数で
   割り算されるので、M を増やせば薄まる。`PREFILL_GROUP` は MoE の行を複数
   チャンクぶん連結する仕組みで、**まさにこれをやる機構**。既定 4 に対し 8 で
   **prefill -1.1%** の兆候が出たまま保留になっている。新しい仕組みは要らず、
   既にある定数を数字で確定させるだけ。**最初にやること。**
2. **なぜ M タイルが小さいのかを見る。**prefill では重みタイルを 1 回展開して
   M 行ぶん使い回せるので、逆量子化は本来ほぼタダのはず。高いということは
   MLX の qmm が M タイルごとに展開を繰り返している疑いがある。カーネルの
   タイル形状が見えるなら、そこが 1 の上限を決める。
3. **展開して置く (dequant once + 密 GEMM)。**MoE では厳しい: 2048 トークンに
   top-k 10 だと 512 人がほぼ全員呼ばれ、1 層ぶん 4bit 1.26GB が bf16 で 5GB。
   層をまたいで持てない。attention の射影や lm_head のような密の重みなら
   成立するが、MoE に比べて小さいので取り分が知れている。
4. **パックの仕方を変える。**バイト境界をまたぐビットシフトを避ける等、展開が
   安い並びにする。うちはベイクを自前で持っているので手は入る。ただし品質と
   decode の帯域を触るので **KLD とセット**(受け入れ幅 +0.0005)。

### 根っこにあるトレードオフ (混同しないこと)

| | 律速 | 欲しい重み |
|---|---|---|
| decode | 帯域 | **小さいほど良い** |
| prefill | 計算 | **展開が安いほど良い** |

**逆方向。**8bit にすれば逆量子化は楽になるが、重みが 2 倍になって decode を
直撃する。**1 つの形式で両方は最適化できない。**prefill だけのために形式を
動かす案は、必ず decode 側の実測とセットで判定する。

---

# prefill 側 (17k、対 mlx-serve で唯一残った明確な負け)

decode と違って prefill は **FLOP 律速**なので、下限は帯域ではなく
**密の 4bit 行列積の実測上限 11.2 TFLOPS** で出す。

## まず: いまの部品時間は古い

手元にある解剖は **moe 15.0 / gdn 9.4 / attn 9.0 / hc 5.5 / ple 2.3
(部品和 41.2s、壁 42.7s)** だが、これは **layer-major prefill を入れる前**の
ビルドの数字。現在の壁は 32.4-32.9s (実文プロンプトでは 35.6s) で、
**24% ぶん古い。**

しかも当時は「部品の絶対時間」しか出しておらず、下限と効率を入れていない。

**最初に書いた効率表は誤っていた (fable-advisor 指摘、訂正済み)。**
attention の下限を「QSA 込み」(1 クエリ最大 budget 列) で数えたが、
**sdpa は加算マスクを渡されても密の全スコアを計算する** -- これは他ならぬ
このリポジトリの実測で確定している (`_vendor/qwen4_exp.py` の該当コメント)。
つまり現行実装が実際に回している FLOP は密の因果であって、疎ではない。

| 部品 | 実測 (古い) | いま回している FLOP | 実効 | 疎性を刈れたときの FLOP |
|---|---|---|---|---|
| MoE 48 層 | 15.0s | 88.2 T | 5.9 TFLOPS | — (疎性の話ではない) |
| **attention 12 層** | 9.0s | **62.9 T** (密因果 42.6 + 射影 20.3) | **7.0 TFLOPS** | **30.0 T** |
| GDN 36 層 | 9.4s | 70.9 T | 7.5 TFLOPS | — |
| HC | 5.5s | 未算出 | — | — |
| PLE | 2.3s | 未算出 | — | — |

**読み方が 180 度変わる。**attention は「実装効率が悪い」のではなく
(7.0 TFLOPS は MoE の 5.9 より上)、**QSA の疎性が計算削減に一切化けていない。**
密 62.9T を 9.0s で回していて、疎性を実際に刈れれば 30.0T で足りる。
**差は最大 4-5s** で、これが prefill に残る単一で最大のレバー。MoE に残る
上積みより大きい。

**合計の「効率 41%」は使わないこと** (fable 指摘)。11.2 TFLOPS は 4bit qmm の
実測上限であって、sdpa のスコア (fp16 稠密) にも GDN の再帰 (行列積ではない)
にも別の天井がある。部品別の結論 (MoE は曲線の上、attention は刈り残し、
GDN はほぼ天井) は生きるが、1 つの数字に潰さない。

## K=640 は無罪だった (2026-09-02)

`mx.gather_qmm` を実形状 (E=512、N=2560、x=(行数,1,K)、transpose=True、
sorted_indices=True) で K を振った:

| 1 専門家あたり 160 行 (G=4 相当) | TFLOPS | 640 換算 |
|---|---|---|
| K=512 | 10.76 | — |
| **K=640** | **10.74** | 基準 |
| K=1024 | 10.97 | **-2.1%** |

**K による差が無い。**1024 に詰めても有効な仕事あたり 2% しか良くならず、
FLOP 1.6 倍を払う価値はゼロ。**「K%512!=0 で qmv_fast から外れる」は
この形状では実測に出ない。**相手のソース読解から持ってきた知見だが、
うちの実形状には当てはまらなかった。

**そして micro で 10.8 TFLOPS = 天井 11.2 の 96% 出ている。**in-model の
MoE は 47.6%。**行列積そのものは速く、遅いのはその周り** (ルータ、並べ替えと
gather、SwiGLU、書き戻しの scatter、共有専門家)。段 P2 の残りはそこ。

## 段 C の結果 (2026-09-02)。**式は 2 勝 2 敗。下限の定数が 1 つ怪しい**

`tools/calibrate.py` (原始量の計測) と `mlxturbo/calibration.py` (式) を作り、
M3 Max で実測 (`bench/results/calibration-m3max.json`)。

### 原始量

    B  連続読み  350.0 GB/s (コピー) / 368.2 GB/s (読み専)
    G  飛び飛び  head_dim 256: 0.548 / 128: 0.578 / 64: 0.591 (密度 0.25)
    L  起動 1 回  2.07 us
    F  密 4bit qmm  11.67 TFLOPS
    r  gather_qmm  5行:0.19 40:0.65 160:0.89 320:0.91  膝は 160
    D  グラフ構築  12.90 us/層

### 式の判定

| 閾値 | 較正値 | 既定 | 判定 |
|---|---|---|---|
| gather の比 `u*` | 0.245 (kv=17k) → 0.262 (kv→∞) | 0.20 | **合う** |
| `MLXTURBO_PREFILL_GROUP` | 4.0 (膝の幅で 2.0-4.0) | 4 | **合う** |
| `MLXTURBO_STAGE_EVERY` | `L/D` = 0.16 → 1 | 2 | **式が外れ** |
| `fast_qmm.M_MIN` | 1.12 | 3 | **式が外れ** |

**`u*` の判定は丁寧に読むこと。**式が当てるべき相手は既定 0.20 ではなく、
掃引で出した**ゼロ交差 0.23** (既定はそこから安全側に 1 割倒した値)。較正値は
交差点に対して +6.5% (17k) 〜 +14% (kv→∞) で、宣言した反転条件「2 割以上
ずれたら式を捨てる」に照らして**式は生きている。**起動項が kv で効く向きも
実測と整合 (短い文脈ほど閾値が前に来る)。

**`STAGE_EVERY` は式が外れ。**式は「1 層ごとに投げろ」と言うが、**同日の
段 X2 で 1 を実測して +0.6%/+0.3% の悪化を確認済み。**式が前提にしている
「起動費用 L だけが効く」が成り立っていない (投げるたびに何か別の費用がある)。
掃引の値を既定にする。

**`fast_qmm.M_MIN` も式が外れ。**4bit だと式が `1/(1 - 4B/F)` に縮んで形に
ほぼ依らなくなり、qkv と lm_head で同じ 1.12 が出る。「MMA が M=3 で `F` に
届く」という仮定が成り立っていない。掃引の値を採る。

### 下限に使っている 393 GB/s が怪しい

**このプログラムの下限計算は一貫して 393 GB/s を使ってきたが、実測は
350 (コピー) 〜 368 (読み専)。7-12% 楽観的だった。**

MoE / GDN / attention はどれも FLOP 律速なので結論は動かないが、**バイト律速の
部品 (embed、および MoE の中身割りで数えた「動いたバイト」) の効率は、実際より
悪く見えていた。**下限を出し直すときは 368 (読み専) か 350 (読み書き) を
用途で使い分けること。

計測で見つかった注意 2 点 (docstring に記録済み):
- **コピーを鎖にすると min が理論値を超える** (chain 1/4/8 で 345/399/435 GB/s)。
  中央値は動かないので `chain=1` で測る
- **`G` は集める密度にも依る** (hd256 で密度 0.125/0.25/0.5 → 0.522/0.548/0.565)。
  純粋なマシン定数ではないので、閾値の近傍 0.25 で測った値を式に入れ、
  密度依存は `G_by_frac` に残した

### 読み込み口

`MLXTURBO_CALIBRATION=<json>` を指定したときだけ読む。**既定の探索パスは
持たせていない** — 較正が本番の挙動を黙って変えないため。指定が無ければ
既定のままで「較正プロファイルが無いので M3 Max の実測値を使う」と一度ログに
出る。優先順は env > 較正 > 実測表 > 保守側。**既定値は 1 つも動かしていない。**

較正値は**交差点**であって安全幅を含まない。採用するなら derate を別に決める。

### 式にできていない閾値

- 受理率依存の 4 つ (文書が「式にしない」と宣言済み): depth の境界、
  `PRIME_WINDOW`、`MLXTURBO_SORT_MIN`、`MLXTURBO_DRAFT_RERANK`
- `MLXTURBO_GATHER_TILE=256` — 17k の掃引 1 条件で決めた値。式が無い
- `fast_qmm.N_MIN=4096` — **コア数を原始量に足せば式にできる**
- `_PREFILL_TAIL_CHUNKS=1` — LCP のヒット率次第で、マシンでは決まらない

## 段 4 の再測 (2026-09-02)。**+495% は測り方の誤りだった**

`--knob wide --only long --ctx 17000` (**`--prefill-once` を外して**再測):

| | 旧 (`--prefill-once` あり) | 新 (なし) |
|---|---|---|
| ms/round A (WIDE on) | 1189.8 | **48.2** |
| ms/round B (基準) | **199.8** | **48.2** |
| 判定 | +495% | **-0.0%** |
| prefill_s | (0、使い回しのため測れない) | **+70.2%** (37.2 → 63.3s) |

**基準そのものが 4 倍違う。**199.8ms/round は他のどの測定 (37-48ms) とも合わない。

**原因は `--prefill-once`。**共有 prefill は `variants[0]` (= A、WIDE on) で
1 回だけ組まれ、B はその状態から再開する。**WIDE は射影の形を変えるので、
別の変種で組んだキャッシュから再開したら比較が成立しない。**

**`--prefill-once` は「その knob が prefill に影響しない」ときにしか使えない。**
`decode_ab.py` は `prefill-group` と `stage-every` を明示的に弾いているが、
**`wide` と `gdn-blocked` も同じ性質**だった (後者は実装時に弾くよう足してある)。

### 結論は変わらないが、理由が違う

**WIDE は decode では中立 (-0.0%)、prefill で壊滅的 (+70.2%)。**既定 off の
ままだが、旧記録の「連結で qmv のカーネル変種が変わり加算順が最終 ulp を動かす
疑い (tok/step 2.44 → 2.23)」も、**+495% も、どちらも実態ではなかった。**
tok/round は -0.2% で動いていない。

**GDN の射影 4 本 (GDN 部品の 53% = prefill 全体の約 13%) をどうするかは、
連結では解けない。**別の手が要る。

## ブロック化 GDN は負けた。**そして 4.3s の見積もりが誤りだった** (2026-09-02)

上の「段 P3 の見送りを撤回する」で「取り分 約 4.3s」と書いたが、**誤り。**
**分母の読み違いがもう一段あった。**

### 何を読み違えたか

`tools/prefill_anatomy.py:514` の `gdn` は **`GatedDeltaNet.__call__` 全体**を
包んでいる。1 層 S=2048 で内訳を測ると:

| | ms | 割合 |
|---|---|---|
| **再帰スキャン** | **6.7** | **20.2%** |
| 射影 4 本 (`_project_in`) | **17.5** | **53%** |
| out_proj + RMSNormGated | 8.2 | 25% |
| 合計 | 33.0 | (部品和 32.4 ≈ 壁 33.0、2%) |

**再帰スキャンは GDN 部品の 2 割しかない。**「1034-1122ms の半分」は成立しない。
スキャン全体でも 36 層 x 6.7 = **241ms/チャンク**で、相手の 2 倍が出ても取り分は
**8 チャンクで約 1s。**

しかも宣言した判定基準「GDN の部品時間が 2 割以上縮まなければ不採用」は、
**スキャンをゼロにしても届かない基準**だった。

相手の 14.9 vs 29.7ms/層 @16K も、2048 幅に直すとうちのスキャン 6.7x8 = 54ms と
桁が合う。**あちらもスキャンだけの数字。**

### 実装は正しく、しかし 1.68 倍遅い

`mlxturbo/kernels/gated_delta_blocked.py` (既定 off)。ブロック長 C を測って
C=32/64/128 で 12.20/11.21/13.53ms、**既定 C=64**。

| | ms |
|---|---|
| 逐次スキャン | **6.7** |
| ブロック化 C=64 | **11.2 (1.68 倍)** |

**理由は MLX の op 単位という形態から来る。**逐次カーネルは状態 64KB を
レジスタに置いたまま 2048 歩進むのでグローバル往復ゼロ。ブロック化は C×C と D を
ブロックごとに書いて読み直す。加えてブロック間の直列 32 回はバッチ 48 の小さな
行列積で、実測 fp32 3.7 / bf16 6.9 TFLOPS 止まり (密は 11.5/12.9)。

**勝つには融合 Metal カーネル (simdgroup_matrix) が要る。**状態を 1 threadgroup
(1024 スレッド、16 float/スレッド) に置き、C×C と逆行列を threadgroup メモリで
回す形。**op 単位の書き換えでは届かない。**

### 数値は問題なかった

ブロック化の上乗せは**相対 1e-6 の桁で、bf16 の分解能 4e-3 より 3 桁下。**
bf16 入力では逐次版と区別が付かない (どちらも対 ops 誤差 max 4.347e-02 /
rms 1.661e-03 で同値)。`tools/verify_gdn_blocked.py` が突き合わせを持つ。

### 実装は捨てずに残す

融合カーネルを書くときの**参照実装と正しさの基準**として使える。既定 off、
docstring の冒頭に負けの記録あり。

### 発火の表 (どの呼び手が prefill 幅で通るか)

| 呼び手 | 発火 |
|---|---|
| `spec_flash._group_prefill_forward` | 6 層 x チャンク数 |
| `capture(light=True)` + `model.model(chunk)` | 通る |
| `_staged_forward` (S=64) | 通る |
| `_staged_forward` (S=1) | 0 |
| `capture(light=False)` (検証フォワード) | **0** |
| **`SpecEngine.generate()` で prefill** | **0** |

**`capture(light=False)` は `GatedDeltaNet.__call__` ごと差し替える写しなので、
そこにはこの分岐が無い** (rollback 用の `states_all` が要るので入れられない)。
**`generate()` で prefill を流すハーネスは発火 0 になる。**`tools/decode_ab.py` は
`generate_stream` を使うので発火する。

### GDN の時間は射影にある

スキャンが 20% なら、**射影 4 本の 53% (= prefill 全体の約 13%) がGDN の本体。**
`MLXTURBO_WIDE` (連結射影) は段 4 で decode の ms/round +495% と決着済みだが、
**あれは decode の数字。**prefill 幅で射影をどうするかは別に測っていない。

## 段 P3 の見送りを撤回する (2026-09-02)。**prefill の実装差はアルゴリズムだった**

段 P0 で GDN の効率が 68-76% と出たので「伸びしろ 2.5s、割に合わない」として
見送った。**その判断が誤り。**

### 何が違うか

うちの GDN カーネル (`mlx_lm/models/gated_delta.py` の
`_make_gated_delta_kernel`) の中身は **`for (int t = 0; t < T; ++t)` の逐次
スキャン**。prefill では T=2048 なので、threadgroup ごとに **2048 回の逐次
反復**を回している。

相手は `gated_delta_blocked_seq` (`transformer.zig:199-437`) を持ち、条件は
`seq_len >= 64` かつ `Dk == 128` — **prefill 幅専用のブロック化スキャン。**
コメントの実測は **14.9ms vs 29.7ms/層 @16K (約 2 倍)。**

**線形 attention の再帰は、rank-1 更新の連鎖 (逐次) でも、ブロック内を行列積に
まとめた形 (ブロック化) でも計算できる。FLOP 数は同じ桁でも、行き先の
ハードウェアが違う。**逐次はスカラー FMA しか使えず、ブロック化は matmul の
ユニットに乗る。**2 倍は FLOP を減らした結果ではなく、使えるハードウェアが
増えた結果。**

### 「効率 68-76%」は誤読だった

段 P0 の下限 762ms/チャンクは **FLOP を素直に割った値**で、**逐次スキャンが
到達できない速度を分母にしている。**効率が高く見えるのに、**アルゴリズムの
天井そのものが機械の天井よりずっと下にある。**

**証拠**: 相手の 14.9ms/層 x 36 = 536ms/チャンクは、**うちの下限 762ms より下。**
下限を割っている時点で、下限が違う仕事を仮定している。

### 取り分

GDN は 1 チャンク 1034-1122ms、8 チャンクで **約 8.6s** (17k prefill 全体の
約 25%)。2 倍なら **約 4.3s。**

**今日特定した中で最大の項。**しかも性質が違う:

| 項 | 大きさ | 減らせるか |
|---|---|---|
| **GDN の逐次スキャン** | **約 4.3s** | **別のアルゴリズムで書けば減る (実例あり)** |
| タイルの水増し | 約 4.6s | 構造的 (ルーティングの分布)。減らしにくい |
| 48 層まとめの上乗せ | 約 4.5s | 未解明。減らせるか不明 |
| 会計漏れ | 約 0.6s | そもそも伸びしろではない |

### 教訓

**効率の分母を「その実装が到達しうる速度」ではなく「機械の天井」で取ると、
アルゴリズムの選択ミスが効率の数字に出ない。**68-76% は「よくやっている」に
見えるが、**逐次スキャンである限りそれ以上は出ない。**

今日 4 回目の「分母が違う」。MoE の 47.6% (qmm の FLOP しか数えていない)、
gather_qmm の 96% (一様分布のマイクロ)、バイト律速の効率 (393 GB/s が
実測 350-368)、そしてこれ。

## GDN の射影も札ではなかった (2026-09-02、卓上計算)

「GDN の射影 4 本 = GDN 部品の 53% = prefill 全体の約 13% = 約 4.6s」を
**唯一の大きい未探索の札**と書いたが、**効率を計算していなかった。**

`hidden=2560` から 4 本が出て、出力幅の合計は **16480**
(`mixed_qkv` 10240 = key_dim 2048 x2 + value_dim 6144 / `z` 6144 / `b` 48 / `a` 48)。

| | |
|---|---|
| 1 層 1 トークン | 2 x 2560 x 16480 = **84.4 MFLOP** |
| S=2048、36 層 | **6.22 TFLOP/チャンク** |
| 下限 (11.2 TFLOPS) | **555 ms** |
| 実測 (17.5ms x 36) | **630 ms** |
| **効率** | **88%** |

**伸びしろは 75ms/チャンク = 8 チャンクで 0.6s。**4.6s ではない。

**WIDE (連結) が prefill で +70% だった理由も、これで説明が付く。**88% 出て
いるものを連結しても取り分は 12% しか無く、連結のために作る中間や、
`N` が変わって qmm のカーネル変種が変わる費用がそれを軽く超える。

### 「4.6s の札」は 3 回とも同じ誤りだった

| 私が札と呼んだもの | 実測・卓上 |
|---|---|
| MoE のタイル水増し 4.6s | **1.4-1.7s** (転嫁率 0.30-0.44) |
| GDN の逐次スキャン 4.3s | **約 1s** (スキャンは GDN の 20%) |
| **GDN の射影 4.6s** | **約 0.6s** (効率 88%) |

**3 回とも「部品の時間」を「伸びしろ」と呼んだ。**部品が大きいことと、
そこに削れるものがあることは別。**効率を出す前に札に数えない。**

## 「負けたから off」の再審査、5 件の結果 (2026-09-02)

A/B の下駄 (長文脈で A 側 +5.6%) を直したので、`CLAUDE.md` に「in-model 実測で
負けたから off」と記録されている knob を測り直した。

| | 旧記録の大きさ | 測り直した結果 |
|---|---|---|
| `rms_norm_gated` | ~0 (空振り) | 判定変わらず (tok/round -1.7% で負け) |
| `moe_route` | +0.34ms (小) | 判定変わらず (+2.6%) |
| `MLXTURBO_HC_PREFILL` | 小 | 判定変わらず (+0.3%/+0.1%) |
| **`MLXTURBO_WIDE`** | **+495% (大)** | **理由がまるごと違った** (decode 中立、prefill +70%) |
| **`MLXTURBO_PIPELINE`** | 大 | **判定変わらず** (短 +49.3% / 長 +34.6%) |

**下駄で結論が変わったものはゼロ。**変わったのは WIDE の**理由**だけで、
それは下駄ではなく **`--prefill-once` の誤用**が原因だった (共有 prefill を
variants[0] で組むので、prefill に影響する knob では比較が成立しない)。

**`CLAUDE.md` の決定則「作って捨てる遅延グラフは規模を問わず MLX の暗黙 eval に
罰される」は、綺麗なハーネスで裏付いた** (楽観パイプラインが +35-50%)。

### 残り 3 つの選別

- `MLXTURBO_MOE_GLU` / `MLXTURBO_PREFILL_CHUNK` — **優先度低。**負けの記録が
  大きいか中立なので、5.6% の下駄では動かない。加えて `PREFILL_CHUNK` は
  モジュール読み込み時に env を読むので 1 プロセス内で切り替えられない
- **`MLXTURBO_FAST_QMM` — 測った (2026-09-02)。謎ではなかった。**

  | | 結果 |
  |---|---|
  | 短文脈 (depth 2 = M 3、窓に入る) | **+0.4%** |
  | 長文脈 (depth 1 = M 2、**窓の外**) | **+0.0%** |

  **長文脈は完全に 0.0% で、予想どおり発火していない** (`M_MIN=3`)。短文脈は
  発火するがほぼ中立。**記録の「適格なのに in-model で負ける」という劇的な話は
  実測に出ない。**

  単体では qkv -22% / lm_head -33% が出るのにラウンドでは +0.4%。
  **今日 4 回見た形と同じ** (`gather_qmm` の 96%、HC の効率 52%、GDN スキャンの
  20%、そしてこれ)。**単体の測定が示すのは「その op の中での改善率」であって、
  「ラウンドへの寄与」ではない。**謎ではなく、その op がラウンドの中で小さい
  だけだった。

### 再審査キャンペーンの結論 (6 件)

**下駄 (5.6%) で結論が変わったものはゼロ。**変わったのは `WIDE` の理由だけで、
原因は下駄ではなく `--prefill-once` の誤用だった。

**`CLAUDE.md` の「in-model 実測で負けたから off」は、6 件とも正しかった。**
記録の理由が違っていたものが 2 件 (`WIDE` の +495%、`FAST_QMM` の「謎」)。

## HC は決着 (2026-09-02)。**既定が正しく、伸びしろを取る手が無い**

段 P0 で「HC は prefill の 10%、効率 52-55% で prefill 最低、伸びしろ 1.65s」と
出たので測り直した。`--knob hc-prefill` の 3 変種
(A = prefill 用の 1 ディスパッチ版 / **C = 現行の既定** (読み側カーネル on、
prefill 融合 off) / B = HC カーネル全部 off)。

| | 短 ms/tok | 長 ms/tok | 短 tok/round | 長 tok/round |
|---|---|---|---|---|
| A (prefill 融合) | +0.3% | +0.1% | 0.0% | 0.0% |
| **C (現行の既定)** | **基準** | **基準** | **基準** | **基準** |
| B (カーネル全部 off) | **+3.3%** | **-0.7%** | **-2.7%** | **+1.6%** |

**既定 C が正しい。**短文脈でカーネルを切ると 3.3% 遅くなり、長文脈で 0.7% 良く
なる分より大きい。**`MLXTURBO_HC_PREFILL` の「負けたから off」も確認できた**
(+0.3% / +0.1% で買うものが無い)。今度は下駄を取ったハーネスでの判定。

### tok/round の符号が文脈で反転する

短文脈は**カーネル on の方が受理率が高い** (2.248 対 2.188)、長文脈は**逆**
(1.666 対 1.693)。数値の変化がテキストによって有利にも不利にも出る形で、
**どちらか一方だけ測っていたら反対の結論になっていた。**
CLAUDE.md の「tok/step は複数プロンプト平均でだけ比較する」がここでも効いた。

### 札は降りる

**紙の上の伸びしろ 1.65s は実在するが、それを取る手が無い。**prefill 融合は
買えず、カーネルを外すのは短文脈で損。**効率が低い = 伸びしろがある、では
ないことの 3 例目** (MoE の 47.6%、GDN の 68-76% に続く)。

## タイル水増しの因果を測った (2026-09-02)。**取り分は 4.6s ではなく 1.4-1.7s**

「MoE のタイル水増し 4.6s」は**水増し率が 1:1 で時間になると仮定した数字**だった。
測ったら転嫁率は 0.30-0.44 で、**取り分は 3 倍過大だった。**

### 実験 (有効な行数を固定し、水増し率だけを変える)

合計 20480 行、E=512、N=2560、K=640。同じ仕事量で専門家ごとの並びだけが違う。
1 プロセス内で回文順に交互。

| 条件 | 中身 | 水増し率 T=32 | 時間 | 比 |
|---|---|---|---|---|
| 倍数ぴったり | 384 人 32 行 + 128 人 64 行 | **1.000** | 6.44 ms | **1.000** |
| 均等 40 | 全員 40 行 | **1.600** | 7.62 ms | **1.182** |
| 裾が重い | 511 人 1 行 + 1 人 19969 行 | **1.775** | 8.65 ms | **1.342** |

**水増し率の順に時間が並ぶ。因果は本物。**T=32 の列が順序を予測し T=64 の列は
しないので、**効いているタイルは T=32** も確定。

### ただし転嫁率は 0.30-0.44

水増し 1.60 → 時間 1.18、水増し 1.78 → 時間 1.34。**60% 水増ししても時間は
18% しか増えない。**

実ルーティングの水増しは 1.40 (段 P0 の中身割りで実測) なので、内挿すると
時間比は **1.12-1.18**。qmm は 8 チャンクで 11.5s なので **取り分は 1.4-1.7s**
(prefill 全体の 4-5%)。

### 副産物: 均等の方が水増しが大きい

**均等 40 行の水増し率は 1.60 で、実ルーティングの 1.40 より悪い。**裾が重い
分布では大きい方の専門家で相対的な水増しが小さくなるため。
**「裾が重いから水増しする」という説明は方向が逆だった。**

### 判断

**札としては残るが、優先度は下がる。**1.4-1.7s を取るには MLX の `gather_qmm`
を 12-18% 上回る ragged カーネルが要る。2026-09-02 の戦績はカーネル 5 本中
生き残り 1 本 (HC 書き戻しの -0.8%)。**prefill 全体の 4-5% のために、その勝率で
カーネルを書くかは別の判断。**

**棚上げ時の見積もり「上積み ≤8%」は、むしろ当たっていた** (qmm に対して
12-18%、prefill 全体では 4-5%)。今日「測る前の見積もりだから疑わしい」と
書いたが、**疑うべきだったのは私の 4.6s の方だった。**

## 「48 層まとめの謎」は測定の作りが甘かった (2026-09-02、検証で判明)

上の「残った謎: 48 層を 1 eval の項」(chunk 0 で +364、chunk 7 で +757、
8 チャンクで 2.9-6.1s) について、**メモリ圧の仮説を検証したら棄却され、
同時に元の数字が再現しなかった。**

### メモリ圧の仮説は直接否定された

`tools/probe_moe_pressure.py` (新規)。chunk 0 (kv=2048) でダミーの常駐を
0 / 0.55 / 2 / 4 / 8 / 16 GB と振り、48 層 1 eval を 1 プロセス内で交互に測った。

- **kv 相当 (0.55GB) のダミーで chunk 0 は 1.0ms しか動かない。**27 倍の 16GB でも
  動かない (-13.4ms)
- **`cache_memory` はダミーとともに 6.1 → 2.6 GB へ確実に縮む** (キャッシュ済み
  バッファが実際に吐き出されている)。**アロケータは反応しているのに壁時計が
  変わらない。**「キャッシュの追い出しに時間が食われている」は直接否定された
- **1 eval のメモリ足跡は kv に依存しない。**`peak - active` が chunk 0 で 5.8GB、
  chunk 7 でも 5.8GB。「chunk 7 では中間が余計に生きている」も違った
- MLX の `memory_limit` は 130.6GB、active 103.6GB で**余裕 27GB**。kv の伸び
  (0.6GB) はその 2%。**越えるべき膝が存在しない**

### 元の数字が再現しない

| | 記録した値 | 検証時 |
|---|---|---|
| 層ごと合計 chunk 0 → 7 | 1720 → 1572 (**減る**) | 1697 → 1773 (**増える**) |
| 48 層 1 eval chunk 0 → 7 | 2085 → 2329 | 1951 → 2144 |
| 差 chunk 0 → 7 | +364 → **+757** | +253 → **+371** |

**「層ごと合計が kv とともに減る」という、謎の出発点だった向きが逆に出た。**

理由: **同じ chunk 0 の層ごと合計が run 間で 131ms (7.7%) ずれる。**雑音の床は
96ms (常駐が同じゼロの 3 条件のばらつき)。**元の「+757」は reps 3 の単発値。**

### 切り分け

- **再現する**: chunk 0 の「1 eval - 層ごと」= +253 / +239。**これは実在する**
  (孤立計測が重なりを捨てるぶんで、符号も期待どおり)
- **再現しない**: その kv 依存。**「謎」だったのはこちら**

### したがって

**「2.9-6.1s の未解明」は札から降ろす。**実在するのは chunk 0 で約 250ms/チャンク
= 8 チャンクで約 2s の「孤立計測との差」で、これは異常ではなく孤立計測の性質。
kv とともに増えるという主張には**標本が足りていなかった。**

**xctrace には行かない。**行く前に、この項をちゃんと標本を積んで測り直すのが先。
ただし雑音の床が 96ms で追う量が同じ桁なので、**その測り直し自体の費用対効果が
低い。**他の札 (MoE のタイル水増し 4.6s、GDN の射影 4.6s、HC の 1.65s) の方が
大きく、機構も分かっている。

## MoE の中身割り (2026-09-02)。**「8.9s の伸びしろ」は大半が伸びしろではなかった**

`tools/prefill_anatomy.py` を拡張して MoE 1 層の中を割り、**バイト会計**
(最小バイトと、コードが実際に作る配列を op ごとに数えた動いたバイト) と
**積み上げ** (qmm 3 本 → +SwiGLU → +並べ替え → +書き戻し → +router 等) を
1 プロセス内で測った。

### 前提が 2 つ崩れた

**1. 「マイクロで天井の 96%」は違う入力分布の値だった。**

マイクロは 1 専門家あたり 40/160/320 行を**一様に**与えている。実際の
ルーティングは 20480 行を 512 人に配る**裾の重い分布**。タイルの水増し率
`sum(ceil(c/T)*T)/sum(c)` の実測は **T=32 で 1.40 倍、T=64 で 1.90 倍**
(3 チャンクとも同じ)。実行 FLOP が有効 FLOP の 1.4 倍なら、**有効仕事あたりの
効率は 71% が上限。**実測 59-67% はその中に収まり、**水増しを入れた下限
(863 x 1.40 = 1208ms) に対して qmm は 84%。**

**`gather_qmm` は遅くない。ルーティングの分布がタイルを余らせている。**
**「47.6% を 96% に」は目標として成立しない。**

**2. バイト会計の漏れは実在したが小さい (fable の反転条件 (i) が発火)。**

`combine` の `us * w[..., None]` が bf16 x fp32 で **fp32 に昇格**し、
2048x10x2560x4 = 210MB を実体化して `sum` が読み直していた。動いたバイトは
最小の 5 倍 (5.5 → 27.7GB)、効率 **9.8% → 49%**。異常値ではなく普通の
帯域律速。`sort` も 43% → 79% (添字を 4 枚作る)。

**ただし総額 +70ms で、1113ms のギャップの 6% しか説明しない。**
fable の反転条件「追加ぶんが 3 割未満なら (c) ではない」が発火した。
**「バイト移動を削る」は主筋ではない。**

### chunk 0 の 2085ms の内訳

    2085 = 1106 (説明できる下限)
         +  575 (qmm の超過 -- 大半はタイルの水増しで説明が付く)
         +   39 (その他の超過)
         +  364 (48 層を 1 eval にまとめた上乗せ)

**非 qmm 部品の在庫は薄い。**個別計測では 400ms あるが、graph の中では
107-282ms/chunk しかない (積み上げで確認)。**qmm 3 本だけの段では積み上げと
部品和が一致する** (chunk 0 で差 -4ms)。

### 残った謎: 「48 層を 1 eval」の項

| | chunk 0 | 4 | 7 |
|---|---|---|---|
| 層ごとに測った合計 | 1720 | 1648 | **1572** (減る) |
| 48 層を 1 eval | 2085 | 2103 | **2329** (増える) |
| 差 | +364 | +455 | **+757** |

**MoE の仕事は kv 非依存なのに、この項だけ kv とともに増える。**
8 チャンクで 2.9-6.1s。タイル水増しと同じ桁。中間が同時に生きる量
(1 層で xs/dn/us 各 105MB) 以外に手掛かりが無く、**この道具では切り分けられ
なかった。**

**次はここ。**道具が足りないので xctrace の GPU タイムライン (段 6 で使う予定の
もの) が要る。メモリ圧かキャッシュの追い出しの形をしている。

### 副次的に読めたもの (小さいが記録)

- `router` は `x.astype(mx.float32)` の写しを 1 枚作ってから qmm に渡す
  (動いたバイトが最小の 4 倍)
- `shared_expert` は `SwitchGLU` と違い silu と mul が別 op
  (`MLP.__call__` が `mx.compile` を通っていない)

どちらも下限側が動かない (FLOP 律速のまま) ので効率の数字には出ない。

## 段 P0 の結果 (2026-09-02、チャンク 0。**ここが最新**)

`tools/prefill_anatomy.py`、ctx=16867、chunk=2048、完全チャンク 8、reps 3、G=4。
出荷経路と同じ融合構成。

| 部品 | 実測 ms | 割合 | FLOP T | 下限 ms | 効率 |
|---|---|---|---|---|---|
| **MoE 48 層** | **2041.5** | **53%** | 10.89 | 972.1 | **47.6%** |
| GDN 36 層 | 1006.1 | 26% | 8.54 | 762.5 | 75.8% |
| **HC 97 回** | **422.7** | **11%** | 2.62 | 233.9 | **55.3%** |
| **Attention 12 層 (indexer 込み)** | **352.9** | **9%** | 3.07 | 273.8 | **77.6%** |
| PLE 1 層 | 24.4 | 1% | 0.13 | 12.0 | 49.2% |
| embed + tile | 0.4 | 0% | 0.00 | 0.2 | 38.1% |
| 部品和 | 3847.9 | | | | |
| 壁時計 (チャンク主導) | 4037.7 | | | | |
| 壁時計 (layer-major) | 3617.2 | | | | |

**部品和 - 壁時計 = -189.8 ms (-4.7%)。部品でほぼ説明が付く。**

### 「半分が未帰属」は誤り (訂正)

このセッションで何度か「部品和 19-20s に対し壁時計 32-38s、半分近くが未帰属」と
書いた。**あれは古い文書の部品時間を足した結果で、現行ビルドには当てはまらない。**
未帰属を掘れば 2 倍、という筋は消えた。

### 段 P1 の格下げ (「最大のレバー」ではなかった)

段 P1 は「prefill の attention はタイル分割 gather、卓上 4-5s、**最大のレバー**」と
宣言していた。**実測では attention はチャンク 0 で全体の 9%、しかも効率 77.6%。**
9% を 78% から詰めても全体では **2% 程度が上限**で、実測 -0.9% と整合する。

**カーネルの出来ではなく、狙う場所が違った。**仕様は相手のソースから正確に
割れていたのに、そこに時間が無かった。**測る前に書いた代償。**

### 攻める順 (実測に基づく)

1. **MoE (53%、効率 47.6%)。**GDN 並みの 76% まで上げれば 2041 -> 1279ms で
   **prefill 全体の 20%**。構造から読める原因は 2 つ:
   - **専門家 1 人あたり 40 行しかない。**2048 トークン x top-k 10 = 20,480 の
     (トークン, 専門家) 組を 512 人で割ると 40。**40 x 2560 x 640 の細長い
     行列積を 512 回**やっている。逆量子化の費用は使い回す行数で割り算されるので、
     ここで薄まらない。**手は `MLXTURBO_PREFILL_GROUP`** (G=4 で 160 行、
     G=8 で 320 行。**8 で prefill -1.1% の兆候が出たまま保留**)
   - **`moe_intermediate = 640`。**MLX の `gather_qmm` は `K % 512 != 0` だと
     `qmv_fast` の経路から外れる (相手のソース読解で判明済み、未着手)
   **どちらが主因かは未分離。**この解剖ツールは MoE の総和しか出さない。
2. **HC (11%、効率 55.3%)。**attention より大きい。**想定していなかった。**
3. GDN (26%、効率 75.8%)。割合は大きいが伸びしろが小さい。
4. attention (9%、効率 77.6%)。**ほぼ詰み。**

### 未確定 (決定的)

**これはチャンク 0 (kv=2048) のみ。**計測点は [0, 4, 7] と宣言してあり、
残り 2 点は測定待ち。**問いは「QSA が attention を平らに保っているか」。**
block_topk 512 x cr 4 = 2048 列しか見ないので理屈では平らなはずだが、
平らでなければ末尾チャンク (kv=16k) で attention が MoE を抜く。
**上の順番はチャンク 4/7 が出るまで暫定。**

### 副産物

**layer-major が chunk-major より 420.6ms 速い** (3617.2 vs 4037.7)。
既存の `_group_prefill_forward` の取り分が現行ビルドでも生きていることの確認。

## 段 P0. 現行ビルドで解剖し直す (先にこれ。他は全部この上に乗る)

`tools/decode_anatomy.py` は decode 専用 (width=2 の 1 フォワード)。
**prefill 版が無い。**同じ作法 (実キャッシュ、部品和 ≈ 壁の確認、下限と効率を
併記) で prefill 版を書き、現行ビルドで測り直す。

- 部品は moe / gdn / attn / indexer / hc / ple / embed に分ける
  (**indexer を attention から分けること** -- decode では長文ペナルティの 43%
  だった。prefill でも分けないと同じ見落としをする)
- チャンク幅 2048 の 1 チャンクを 1 単位として測る。チャンク番号で費用が
  変わる (kv が伸びる) ので、**先頭・中間・末尾の 3 点**を取る
- HC と PLE の下限も出す (いま未算出)

**反転条件**: なし (測るだけ)。ただし**この結果が出るまで下の段に着手しない。**
古い数字の上に計画を積むと、depth の一件と同じことになる。

## 段 P1. prefill の attention -- タイル分割 gather (卓上 4-5s、最大のレバー)

**「prefill には gather が効かない」という当初の却下は早計だった** (fable 指摘)。

却下の根拠は「1 チャンク 2048 クエリの和集合はほぼ全ブロックになる」だったが、
これは **2048 クエリ全体で見た観測**にすぎない。gather の成否は**タイル幅の
関数**であって、経路そのものの不成立ではない。

budget はクエリあたり 512 ブロック (kv 16852 なら選択率 12%)。**隣り合う
クエリの選択は強く相関する** (局所窓 + 少数のグローバルブロック) ので、
タイル 128-256 なら union が kv の 20-40% に縮む余地がある。

**順序を変えた (実装が小さいので、代理指標より本物を先に測る)。**

当初は「union/kv_len を測ってから実装を決める」段取りにしていたが、
タイルループは既存の `_gather_forward` に for を 1 つ足すだけで、
**実装が代理指標の計測とほぼ同じ重さ**しかない。しかも union 比は代理で、
実際に効くかは gather の帯域コストとタイル毎の重複読みを含んだ壁時計でしか
決まらない -- このリポジトリの作法 (「micro の勝ちを採用の根拠にしない、
in-model で決める」) にも合う。よって:

**P1a (実装)**: `_gather_forward` にタイルループを足す。タイル幅は knob
(`MLXTURBO_GATHER_TILE`、既定 0 = 従来どおり S 全体で 1 回)。
S <= タイル幅なら従来経路に落ちるので decode は影響を受けない。
**新カーネルは要らない** -- `take_along_axis` + 小 bool マスク sdpa という
既存の型をタイルごとに回すだけ。

**P1b (計測)**: 実 17k / 50k で **タイル幅 {0, 128, 256, 512} を掃引**し、
prefill の壁時計で判定する。union 比は `_gather_stats` で併記して、
**速くなった/ならなかった理由を読む**ために使う (判定には使わない)。

**反転条件 (先に宣言)**: 17k の prefill 壁時計が改善しないなら捨てる。
union 比が 0.4 を超えていたのが理由なら、そう記録して畳む
(タイル毎の K/V 重複読みがスコア削減を食っている、という説明が付く)。

和の順序が変わるのでビット一致は失う -> 品質側の判定は in-model の
tok/step + KLD。

**補足**: prefill 側の sdpa には decode で入れた細工 (32 行の壁の分割) が
**何も入っていない** (あの分岐は S<=8 限定)。prefill は 2048 クエリで
加算マスク付きの素の sdpa 1 発。手つかずなのは上の結論と整合する。

### 決着: 相手の実装を読んだ (2026-09-01、`~/dev/mlx-serve` のソース)

**mlx-serve は prefill で疎性を実際に刈っている。**`src/transformer.zig` の
コメントが直球で書いている -- *"Prefill gathers them by index; decode/verify
widths (and a declined gather) run under the bool mask"*。つまり
**prefill は集める / decode は bool マスク**で、今日うちが decode 側の実測で
出した交差点の判断と方向が一致する。

だが**やり方が根本的に違う**。

| | mlx-serve (`gatherQsa256`) | mlxturbo (今日の実装) |
|---|---|---|
| 実体 | **融合 Metal カーネル**。1 threadgroup = 1 クエリ x 1 kv head、`BK`(16/32) 行ずつ threadgroup memory に段取りしながら online softmax | `take_along_axis` -> `sdpa` の**汎用 op 2 段** |
| 中間バッファ | **無い** (カーネル内でポインタ演算で直接読む) | `k_sel`/`v_sel` を**実体化**する |
| gather 後のマスク | **作らない** (読む範囲そのものが可視集合。因果性は読む範囲に埋まっている) | union 幅の bool マスクを作って sdpa に渡す |
| タイルの軸 | **キー方向** (`BK`)。クエリ方向のタイルという概念が無い | **クエリ方向**。タイルごとに union を取り直すので**同じブロックを重複して読む** |
| 降りる基準 | **kv 長の絶対値 8192** (実測ラダー: 8k -3% / 16k +4% / 32k +19% / 64k +42%) | union/kv の比 0.20 (実測: 比 24% +1.1% / 16% -6.7% / 8% -15.4%) |
| dtype | bf16 固定。違えば諦める | cache のまま |

**うちの方式には天井がある。**汎用 op を 2 段重ねる限り、選んだ列を
**書いて、読み直して、マスクを作る**費用が必ず乗る。彼らは読みっぱなしで
書きがゼロ。帯域律速の prefill でこの往復が丸ごと差になる。

交差点の位置もそれで説明が付く -- 彼らは kv 10-12k 付近で黒字化するのに、
うちは 18k 付近 (比 23%)。**実体化の費用が交差点を後ろに押している。**

**判断**: prefill の attention を彼らに並べるには**融合カーネルを書くしかない。**
ただし段 5 の方針どおり、**何を書くべきかが確定したのが成果**である:

- 1 threadgroup = 1 クエリトークン x 1 kv head
- キー方向に `BK`(16 or 32) 行ずつ threadgroup memory へ段取り
- online softmax (スコアを全部実体化しない)
- 中間バッファ無し、gather 後のマスク無し (可視範囲を添字演算で決める)
- bf16、head_dim 256 固定で、外れたら既存経路へ落ちる

**decode 側の gather は捨てない。**S=2 では実体化する量が小さく、50k で
-15.4% と実測で勝っている。「汎用 op の 2 段重ねでは prefill 幅に耐えない」
だけであって、decode 幅では成立している。

## 段 P2. prefill の MoE (卓上 7.1s、効率 53%)

layer-major で 1 エキスパートあたりの行数 r=160 まで上げた後の値。
記録の「segment-aware GEMM カーネルの上積みは ≤8%」は**この上のさらに上積み**の
話で、下限までの 7.1s とは別物。

- `MLXTURBO_PREFILL_GROUP` を 4 から 6/8 に上げると r が 240/320 になる
  (効率曲線は r に単調)。**env だけで測れる** (`--knob prefill-group`、実装済み)
- 代償は checkpoint 粒度が g*2048 に粗くなること
  (`MLXTURBO_PREFILL_TAIL_CHUNKS` で末尾側は守れる)
- **反転条件**: 17k TTFT が改善しない、または追記ターンの warm TTFT が 2% 以上
  悪化したら戻す

## 段 P3. prefill の GDN (卓上 3.1s、効率 67%)

連結射影 (`MLXTURBO_WIDE`) は decode 側で測る予定 (段 4) だが、**prefill 幅でも
効くはず**で、そちらは測ったことがない。P0 の後に `--knob wide` を prefill_s で
判定する形で回す。

## 段 P4. HC と PLE (下限が未算出、紙の上で先に埋める)

合わせて 7.8s = 部品和の 19% を占めるのに、**下限を出していない。**
FLOP とバイトを数えるだけならタダなので、**表の穴を先に埋める** (行動は
その結果次第)。HC は prefill 融合が負けた記録があるが、**あれは「あの実装」の
棄却であって HC の理論下限は未計算**。

**PLE / n-gram の直列化** (fable 指摘、未確認): 計測構成で
`FASTMLX_NGRAM_DISK=1` が有効なら、n-gram 行のディスク読みがチャンク処理と
**直列になっていないか**確認する価値がある。直列なら次チャンク分の先読みで
2.3s の相当部分が消える。無効なら無視してよい。

## 段 P5. indexer を attention から分けて数える (P0 の必須項目)

いまの「attn 9.0s」には **indexer 自身のコストが混ざっている**
(fp32 einsum で (B,S,n_blocks,heads) を実体化 + argpartition)。
タイル gather を入れても **indexer は残る**ので、分けて測っておかないと
gather 化後の残差を読み違える。P0 では attention を
**射影 / スコア / indexer / マスク組み** の 4 つに割ること。

---

# 取り込んだもの (IMPROVEMENT-QUEUE.md から移管、2026-09-01)

`docs/research/IMPROVEMENT-QUEUE.md` は A1 (17k decode の解剖) を最後に
役目を終えた。残っていた 3 項目をここに移す。

## 段 X1. pooled キーの増分キャッシュ (= 旧 B4。段 2 の実測で根拠が出た)

段 2 の内訳 (17k、幅 2、`tools/micro_indexer.py`):

| 段 | ms | % |
|---|---|---|
| **pooled 再構築 (mean + k_layernorm)** | **1.444** | **27.4%** |
| q layernorm+rope と einsum/relu-sum | 1.195 | 22.7% |
| **pooled への rope** | **0.953** | **18.1%** |
| visible + scores + argpartition | 0.871 | 16.5% |
| cache.update(raw_k) | 0.801 | 15.2% |
| keep マスク構成 | 0.498 | 9.5% |
| index_qk_proj + reshape | 0.331 | 6.3% |

**pooled 関連が 45.5%。**ブロックは `compress_ratio` トークン分が揃った時点で
確定し、以後は不変なので、**新しく完成したブロックぶんだけ足せばよい。**

当初「argpartition が主体なら設計変更が要る」と書いたが、実測では 16.5% しか
なく、**素直に増分化するのが正解**だった。

**反転条件**: 増分キャッシュのメモリ (17k で 4213 ブロック x 128 x 12 層 x 2B
= 13MB、50k で 38MB) が問題になる、または in-model で 1% 未満なら畳む。

## 段 X2. 前提が変わったのに固定されている定数 (= 旧 A2/D2)

depth 既定が長文で 1 になったので、**verify 幅が 3 位置 -> 2 位置に減った**。
depth 2 の時代に決めた閾値が軒並み境界の内側に落ちている:

- `fast_qmm` の `M_MIN=3` -> **長文脈ではもう発動しない**
- `MLXTURBO_SORT_MIN=16` に対して添字数が ~30 -> ~20 に減った
- `MLXTURBO_DRAFT_RERANK=1` は**単独 A/B の記録が無い** (複合構成でしか
  測っていない、しかも depth 2 時代)
- `PRIME_WINDOW=2048` は 32k プロンプト 1 条件だけで固定
- `_STAGE_EVERY=2` は掃引が 16->2 で単調改善のまま端点で打ち切り、1 が未測

**反転条件**: 3 プロンプト平均で符号が揃わなければ既定を動かさない。

## 段 X3. スコアボードの再計測 (= 旧 D3)

4k / 17k の値は **depth 既定の変更・QSA 因果性修正・gather attention の前**の
もの。全部入れ終わってから、出荷経路 (`build_runner` を通す) で 1 回測り直す。
`bench/vs_mlx_serve.py` (再現手順をスクリプト化済み) を使う。冷ましてから短く
(熱で 25% 落ちる実測がある)。

**この段は最後**。途中で測ると、そのたびに古くなる。

---


## 段 6 決着 (2026-09-02)。**未帰属 4.89ms は存在しなかった**

`tools/decode_anatomy.py` を 3 run 流して残差の再現性を見た (17k、width=2)。

| run | 部品和 | 壁時計 (段階投入あり = 本番経路) | 差 | 壁時計 (段階投入なし) |
|---|---|---|---|---|
| 1 | 36.16 | 35.04 | **+1.12ms (+3.2%)** | 41.56 |
| 2 | 35.45 | 35.00 | **+0.45ms (+1.3%)** | 41.36 |
| 3 | 35.48 | 34.99 | **+0.49ms (+1.4%)** | 41.71 |

**3 run とも符号が正** (部品和が壁時計を上回る)。孤立計測が重なりを捨てるぶんで
**期待どおりの符号**。本番経路に対する未帰属は無い。

**記録の 4.89ms は「段階投入をしなかった場合の隙間」だった。**段階投入なしの
壁時計 41.4-41.7ms と部品和 35.5 の差が約 6ms で、記録とほぼ一致する。
**文書自身が「壁時計を `_staged_forward` に揃えよ」と警告していた項目で、
その修正は既に入っていた。**

**副産物: 段階投入の取り分が +6.4〜6.7ms/ラウンド = 18%。**decode でいちばん
効いている最適化で、既定で入っている (`STAGE_EVERY=2`、同日の掃引で 1 も 4 も
悪化と確認)。

**これで xctrace のレーンは不要になった。**追うべき隙間が無い。

## このプログラムの答え (2026-09-02)

**問い**: decode と prefill を下限まで詰められるか。

**答え: prefill は既に下限のかなり近くにいる。対 mlx-serve の差は、うちの
無駄ではなく、相手が別の下限で走っていることによる。**

### prefill 17k (35s) の内訳と、残っている伸びしろ

| 部品 | 割合 | 効率 | 実質の伸びしろ | 取るのに要るもの |
|---|---|---|---|---|
| MoE | 48% | 47.6%* | **1.4-1.7s** | `gather_qmm` を 12-18% 上回る ragged カーネル |
| GDN の射影 | 13% | **88%** | **0.6s** | 無い (連結は +70% で否定) |
| GDN のスキャン | 5% | — | **約 1s** | 融合 Metal (op 単位のブロック化は 1.68 倍遅い) |
| attention | 20% | 65-77% | 2.1s | 融合カーネル (実装したが -0.9%) |
| HC | 10% | 52-55% | **0s** | **取る手が無い** (両方の扉が閉まっている) |

\* MoE の 47.6% は下限が qmm の FLOP しか数えていない値。バイト会計を足しても
ギャップの 6% しか説明せず、残りはタイル水増し (転嫁率 0.30-0.44) と
孤立計測との差。

**合計しても 5s 前後で、どれも MLX を上回るカーネルが要る。**2026-09-02 の
打率はカーネル 5 本中 1 本 (HC 書き戻しの -0.8%)。

### 対 mlx-serve の 17k、30.0 対 21.6 秒をどう読むか

**うちに 8 秒の無駄があるのではない。**相手は**別の下限**で走っている:

- `gated_delta_blocked_seq` (`seq_len>=64` の prefill 幅専用): 逐次スキャンを
  ブロック化して matmul のユニットに乗せる。**うちの下限計算は逐次の FLOP を
  仮定しているので、相手はその下限を割れる**
- GDN の前処理を 1 カーネルに畳む (うちは 8 dispatch)
- HC の書き戻しを次の読みに畳み込んで dispatch を 0 にする

**追うなら融合 Metal を面で書く多日レーン。**op 単位の書き換えでは届かない
ことが 2026-09-02 に 5 本で確認された。

### decode 側

達成帯域 393GB/s (実測は 350-368) に対し、読み出しから計算した天井 218 tok/s に
対して実測 44 tok/s。**ただし「幅を広げると +138.6%」なので、GPU は遊んでいない**
(段 G1)。段 6 の未帰属 4.89ms は、prefill の同種の項が単発値の産物だったので
**同じ疑いで標本を積み直すのが先。**

### このプログラムから出た決定則

1. **率は時間ではない。**転嫁率 0.30 なら 60% の無駄は 18% の時間
2. **効率を出す前に「札」に数えない。**部品が大きいことと削れることは別
   (「4.6s の札」を 3 回作って 3 回とも縮んだ)
3. **単体の測定が示すのは「その op の中での改善率」であって「ラウンドへの
   寄与」ではない** (`gather_qmm` 96% / HC 52% / GDN スキャン 20% /
   `fast_qmm` の qkv -22%、いずれもラウンドでは 1% 以下)
4. **効率の分母が何を仮定しているかを確かめる。**機械の天井で割ると
   アルゴリズムの選択ミスが効率の数字に出ない (GDN の 68-76%)
5. **投機デコードでは受理率が品質の指標そのもの。**数値を変えるカーネルは
   `ms/round` ではなく `tok/round` で判定する
6. **道具自体を対照で検査する** (null knob、機構だけの対照 C、発火カウンタ、
   GPU 版の一次検査)

---

# 閾値をマシン非依存にする (段 C: 較正プロファイル)

いま既定に埋まっている閾値は、ほぼ全部 **M3 Max 1 台で測った値**である。
別の機種では動く。「定数の無い式」にはならないが、**式と、そのマシンで測る
少数の原始量に分ける**ことはできる。

## 原始量 (1 台につき 1 回測れば済むもの)

| 記号 | 中身 | 測り方 |
|---|---|---|
| `B` | 連続読みの達成帯域 (GB/s) | 大きい配列のコピー |
| `G` | 飛び飛び読みの効率 (`B` に対する比) | `take_along_axis` を同じ量で |
| `L` | カーネル起動 1 回の費用 (us) | 小さい op を鎖にして差分 |
| `F` | 密 4bit qmm の上限 (TFLOPS) | 大きい `quantized_matmul` |
| `r(rows)` | `gather_qmm` の効率曲線 | 行数を振る (既に `probe_gather_qmm.py` がある) |
| `D` | グラフ構築 1 層あたりの CPU 時間 | 層ループの構築だけを計る |

## 式 (原始量から閾値を出す)

### gather の比はモデルにも依存する (QSA を持つモデルが増えるなら重要)

式が読む `token_budget` / `compress_ratio` はパックから取るので union の
大きさは自動で追随する。だが**比の閾値そのもの**は、gather の効率 `G` に
依存し、`G` は「1 要素あたり何バイト連続で読めるか」で決まる。

KV は `(B, n_kv_heads, kv, head_dim)` なので、kv 軸で集めると連続長は
**`head_dim * 2` バイト**。Flash-Next は 256*2 = 512B。もし `head_dim=128` の
QSA モデルが来れば連続長は半分になり、`G` が落ちて**閾値は前に動く**。

**つまり定数 0.20 は「Flash-Next 系での測定値」であって、QSA を持つ別の
モデルにそのまま持ち込めない。**一般解は下の較正で `G` を直接測り、
`u* = (1 - n*L*B/(kv*bytes)) / (2 + 1/G)` から出すこと。定数はその
フォールバックとして残す (較正結果が無いときはログに出す)。

**gather の比 `u*`** (段 3(b))。密は `kv*bytes/B`、gather は
「集める読み `u*kv*bytes/(B*G)` + 書き `u*kv*bytes/B` + sdpa の読み
`u*kv*bytes/B` + 追加起動 `n*L`」。等号を解くと

    u* = (1 - n*L*B / (kv*bytes)) / (2 + 1/G)

`G≈0.5` なら第 2 項は 4 で、起動項を無視すると `u*≈0.25`。
**M3 Max の実測 0.23 とよく合う。**起動項は kv が小さいほど効くので、
短い文脈ほど閾値が前に来る -- 実測で 17k がちょうど交差点だったのと整合する。

**`MLXTURBO_PREFILL_GROUP`**: `r(rows)` の曲線の膝そのもの。
G を上げると rows が比例して増えるので、`r` が飽和する rows を探して
`rows / (2048 * top_k / n_experts)` から G を出す。**曲線さえ測れば式は要らない。**

**`_STAGE_EVERY`**: 層ループの構築時間 `D` と、`async_eval` 1 回の費用の比。
`every` 層ごとに投げると、隠せる泡は `every * D`、払う費用は `L`。
`every* ≈ max(1, L/D)` が目安。**M3 Max では 2 が最良と出ている。**

**`fast_qmm.M_MIN`**: 密の MMA タイルが stock qmv を上回る M。
stock は M にほぼ比例して重みを読み直すので `M * kv_bytes/B`、MMA は
`kv_bytes/B + FLOP/F`。等号から `M* = 1/(1 - FLOP*B/(F*kv_bytes))`。

## マシンでは決まらないもの (混同しないこと)

以下は**受理率に依存**する = モデルとテキストで決まるので、原始量からは出ない。
機種を変えても値は動くが、それは「マシン依存」ではなく「そのマシンでの
ラウンド費用が変わったから」であって、**測り直しは in-model の掃引でしかできない**。

- **depth の境界**: 1 位置足す限界費用 vs 受理が増える利得。いま
  `indexer_budget` に置いてあるのは**実測の交差点がたまたまそこと一致した**
  からで、機構的な必然ではない。別機種では一致しない可能性がある
- **`PRIME_WINDOW`**: priming の費用 vs 受理率の伸び
- **`MLXTURBO_SORT_MIN`**: ソートの費用 vs 局所性の利得 (添字数に依存し、
  添字数は depth と top_k で決まる)
- **`MLXTURBO_DRAFT_RERANK`**: 粗ヘッドの費用 vs 受理率の維持

## やること

1. `tools/calibrate.py` を書き、原始量 6 つを測って JSON に落とす
2. 実行時にそれを読み、上の式で閾値を出す (無ければ M3 Max の実測値に
   フォールバックし、**その旨をログに出す** -- 黙って他機種の値を使わない)
3. 受理率依存の 4 つは**式にしない**。「掃引が要る」と docs に書き、
   `tools/decode_ab.py` の knob をその手順として残す

**反転条件**: 較正で出した閾値が、実測の掃引で決めた値と 2 割以上ずれるなら、
式が現実を捉えていない。式を捨てて掃引の値を既定にする。

---

## 作法 (この計画に固有のもの)

- **下限は「達成帯域 393GB/s」で計算する。**理論値 400 ではなく実測の到達値。
- **micro の勝ちを採用の根拠にしない。**3 敗が全部それ。採否は in-model の
  複数プロンプト x 512 平均で決める (`CLAUDE.md`)。
- **段 1 の結果が段 2/3 の存在を決める。**順番を飛ばさない。
- 出力が変わる変更 (QSA off、連結射影) は、速度が勝っても **KLD と受理率を
  測ってから**採否を決める。品質を売って速度を買わない。
